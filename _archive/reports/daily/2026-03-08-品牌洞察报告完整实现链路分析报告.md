品牌洞察报告模块 - 完整实现链路分析报告
一、相关文件完整路径清单
1.1 后端核心服务层 (Backend Services)
文件路径
职责描述
/Users/sgl/PycharmProjects/PythonProject/backend_python/wechat_backend/services/report_data_service.py
报告数据聚合服务，负责从各数据源聚合完整报告数据
/Users/sgl/PycharmProjects/PythonProject/backend_python/wechat_backend/services/report_aggregator.py
报告聚合器，计算品牌分数、SOV、风险评分
/Users/sgl/PycharmProjects/PythonProject/backend_python/wechat_backend/services/diagnosis_orchestrator.py
诊断编排器，统一协调所有诊断子流程
/Users/sgl/PycharmProjects/PythonProject/backend_python/wechat_backend/services/report_generation_service.py
报告生成服务
/Users/sgl/PycharmProjects/PythonProject/backend_python/wechat_backend/diagnosis_report_service.py
诊断报告服务层，业务逻辑处理
/Users/sgl/PycharmProjects/PythonProject/backend_python/wechat_backend/diagnosis_report_repository.py
诊断报告仓库层，数据库 CRUD 操作
/Users/sgl/PycharmProjects/PythonProject/backend_python/wechat_backend/diagnosis_report_storage.py
诊断报告存储服务
/Users/sgl/PycharmProjects/PythonProject/backend_python/wechat_backend/diagnosis_report_transformer.py
诊断报告转换器
1.2 后端 API 视图层 (Backend Views/API)
文件路径
职责描述
/Users/sgl/PycharmProjects/PythonProject/backend_python/wechat_backend/views/diagnosis_api.py
诊断报告 API 路由 (/api/diagnosis/report/{execution_id})
/Users/sgl/PycharmProjects/PythonProject/backend_python/wechat_backend/views/diagnosis_views.py
诊断相关视图模块，包含品牌诊断执行逻辑
/Users/sgl/PycharmProjects/PythonProject/backend_python/wechat_backend/views/report_views.py
Report 相关视图模块
1.3 前端服务层 (Frontend Services)
文件路径
职责描述
/Users/sgl/PycharmProjects/PythonProject/miniprogram/services/reportService.js
前端报告数据服务，封装 API 调用
/Users/sgl/PycharmProjects/PythonProject/miniprogram/pages/report-v2/report-v2.js
报告页面 v2 逻辑
/Users/sgl/PycharmProjects/PythonProject/miniprogram/cloudfunctions/getDiagnosisReport/index.js
云函数：获取完整诊断报告
1.4 Node.js 服务层 (Node.js Services)
文件路径
职责描述
/Users/sgl/PycharmProjects/PythonProject/services/reportAggregator.js
报告聚合服务 (Node.js 版)
/Users/sgl/PycharmProjects/PythonProject/services/scoringService.js
战略评分服务，计算 SOV、风险评分、品牌健康度
/Users/sgl/PycharmProjects/PythonProject/services/streamingReportAggregator.js
流式报告聚合器
/Users/sgl/PycharmProjects/PythonProject/services/dataLoaderService.js
数据加载服务
1.5 数据库迁移文件 (Database Migrations)
文件路径
职责描述
/Users/sgl/PycharmProjects/PythonProject/backend_python/wechat_backend/database/migrations/002_enhanced_report_tables.sql
增强报告表结构迁移
/Users/sgl/PycharmProjects/PythonProject/backend_python/wechat_backend/database/migrations/003_fix_report_tables.sql
修复报告表结构迁移
二、关键代码片段分析
2.1 后端 API 入口 (/api/diagnosis/report/{execution_id})
文件: /Users/sgl/PycharmProjects/PythonProject/backend_python/wechat_backend/views/diagnosis_api.py
@diagnosis_bp.route('/report/<execution_id>', methods=['GET'])
@rate_limit(limit=60, window=60, per='user')
def get_full_report(execution_id):
    """获取完整诊断报告"""
    try:
        # 获取服务
        service = get_report_service()

        # 获取完整报告
        report = service.get_full_report(execution_id)

        # 处理报告不存在的情况
        if not report:
            return jsonify({
                'error': '报告不存在',
                'execution_id': execution_id,
                'suggestion': '请检查执行 ID 是否正确，或重新进行诊断'
            }), 404

        # 处理报告为字典但包含 error 字段的情况
        if isinstance(report, dict) and report.get('error'):
            return jsonify(report), 200

        # 验证报告并返回验证信息
        validation = get_validation_service().validate_report(report)
        report['validation'] = validation

        # 转换为 camelCase
        return jsonify(convert_response_to_camel(report)), 200

    except Exception as e:
        api_logger.error(f"获取完整报告失败：execution_id={execution_id}, 错误：{e}", exc_info=True)
        return jsonify({
            'error': '获取报告失败',
            'message': str(e),
            'execution_id': execution_id,
            'suggestion': '请稍后重试或联系技术支持'
        }), 500
2.2 报告服务层核心方法
文件: /Users/sgl/PycharmProjects/PythonProject/backend_python/wechat_backend/diagnosis_report_service.py
def get_full_report(self, execution_id: str) -> Optional[Dict[str, Any]]:
    """获取完整报告（P0-REPORT-2 优化：处理失败场景）"""
    db_logger.info(f"获取完整报告：{execution_id}")

    # 1. 获取报告主数据
    report = self.report_repo.get_by_execution_id(execution_id)
    if not report:
        return self._create_fallback_report(execution_id, '报告不存在', 'not_found')

    # 2. 获取结果明细
    results = self.result_repo.get_by_execution_id(execution_id)

    # P1-1 降级方案：结果为空时的处理
    if not results or len(results) == 0:
        return self._create_fallback_report(
            execution_id,
            '诊断已完成，但未生成有效结果',
            'no_results',
            report
        )

    # 3. 获取分析数据
    analysis = self.analysis_repo.get_by_execution_id(execution_id)
    analysis_data = self._ensure_complete_analysis(analysis, execution_id)

    # 4. 计算品牌分布
    brand_distribution = self._calculate_brand_distribution(results)

    # 5. 计算情感分布
    sentiment_distribution = self._calculate_sentiment_distribution(results)

    # 6. 提取关键词
    keywords = self._extract_keywords(results)

    # 7. 构建完整报告
    full_report = {
        'report': report,
        'results': results,
        'analysis': analysis_data,
        'brandDistribution': brand_distribution,
        'sentimentDistribution': sentiment_distribution,
        'keywords': keywords,
        'meta': {...},
        'validation': validation,
        'qualityHints': {...}
    }

    return convert_response_to_camel(full_report)
2.3 诊断编排器核心流程
文件: /Users/sgl/PycharmProjects/PythonProject/backend_python/wechat_backend/services/diagnosis_orchestrator.py
async def execute_diagnosis(self, user_id, brand_list, selected_models, custom_questions, ...) -> Dict[str, Any]:
    """执行完整诊断流程"""
    try:
        # ========== 阶段 1: 初始化 (事务外 - 仅内存) ==========
        phase1_result = await self._phase_init()
        if not phase1_result.success:
            raise ValueError(f"初始化失败：{phase1_result.error}")

        # ========== 阶段 2: AI 调用 (事务外 - 网络 IO) ==========
        phase2_result = await self._phase_ai_fetching(brand_list, selected_models, custom_questions, user_id, user_level)
        if not phase2_result.success:
            raise ValueError(f"AI 调用失败：{phase2_result.error}")

        # ========== 阶段 3: 结果保存 (事务 A - 短事务) ==========
        phase3_result = await self._phase_results_saving_transaction(phase2_result.data, brand_list, selected_models, custom_questions)
        if not phase3_result.success:
            raise ValueError(f"结果保存失败：{phase3_result.error}")

        # 等待数据库提交完成
        await asyncio.sleep(0.1)  # 等待 100ms，确保数据持久化

        # ========== 阶段 4: 结果验证 (事务外 - 只读验证) ==========
        phase4_result = await self._phase_results_validating(phase2_result.data)

        # ========== 阶段 5: 后台分析 (事务外 - 异步提交) ==========
        phase5_result = self._phase_background_analysis_async(db_results, brand_list)

        # ========== 阶段 6: 报告聚合 (事务 B - 短事务) ==========
        phase6_result = await self._phase_report_aggregating(db_results, brand_list)
        if not phase6_result.success:
            raise ValueError(f"报告聚合失败：{phase6_result.error}")

        # ========== 阶段 7: 完成 (事务 C - 状态更新) ==========
        phase7_result = await self._phase_complete(phase6_result.data)

        return {
            'success': True,
            'execution_id': self.execution_id,
            'report': phase6_result.data,
            'total_time': total_time,
            'trace_id': trace_id
        }

    except Exception as e:
        # 错误处理和日志记录
        return {
            'success': False,
            'execution_id': self.execution_id,
            'error': str(e),
            'error_code': error_code.value.code,
            'trace_id': trace_id
        }
2.4 前端报告服务
文件: /Users/sgl/PycharmProjects/PythonProject/miniprogram/services/reportService.js
async getFullReport(executionId) {
  try {
    console.log('[ReportService] Getting full report:', executionId);

    // 检查缓存
    const cached = this._getFromCache(executionId);
    if (cached) {
      return cached;
    }

    const res = await wx.cloud.callFunction({
      name: 'getDiagnosisReport',
      data: { executionId }
    });

    const report = res.result;

    // 处理空状态和错误状态
    if (!report) {
      return this._createEmptyReportWithSuggestion('报告不存在', 'not_found');
    }

    // 处理失败状态
    if (report.status === 'failed' || (report.success === false)) {
      return this._createFailedReportWithMetadata(report);
    }

    // 处理报告数据
    const processedReport = this._processReportData(report);

    // 缓存报告
    this._setCache(executionId, processedReport);

    return processedReport;
  } catch (error) {
    return this._createEmptyReportWithSuggestion(
      '获取报告失败：' + (error.message || '未知错误'),
      'error'
    );
  }
}
2.5 云函数入口
文件: /Users/sgl/PycharmProjects/PythonProject/miniprogram/cloudfunctions/getDiagnosisReport/index.js
exports.main = async (event, context) => {
  const { executionId } = event;

  if (!executionId) {
    return {
      success: false,
      error: '缺少执行 ID',
      suggestion: '请检查执行 ID 是否正确'
    };
  }

  try {
    const axios = require('axios');
    const response = await axios.get(`${API_BASE_URL}/api/diagnosis/report/${executionId}`, {
      timeout: 30000,
      headers: {
        'Content-Type': 'application/json',
        'User-Agent': 'WeChat-Miniprogram/1.0'
      }
    });

    const report = response.data;

    if (!report) {
      return createEmptyReport('报告不存在', 'not_found', executionId);
    }

    if (report.error) {
      return {
        success: false,
        ...report
      };
    }

    return {
      success: true,
      data: report
    };

  } catch (error) {
    if (error.code === 'ECONNREFUSED') {
      return createEmptyReport('后端服务不可连接', 'server_error', executionId);
    } else if (error.code === 'ETIMEDOUT') {
      return createEmptyReport('请求超时，请稍后重试', 'timeout', executionId);
    } else {
      return createEmptyReport('获取报告失败：' + (error.message || '未知错误'), 'error', executionId);
    }
  }
};
2.6 报告聚合器 (Python)
文件: /Users/sgl/PycharmProjects/PythonProject/backend_python/wechat_backend/services/report_aggregator.py
def aggregate(
    self,
    raw_results: List[Dict[str, Any]],
    brand_name: str,
    competitors: List[str],
    additional_data: Optional[Dict[str, Any]] = None
) -> Dict[str, Any]:
    """聚合品牌诊断报告"""
    # 1. 数据清洗
    cleaned_results = self._sanitize_results(raw_results)
    filled_results = self._fill_missing_data(cleaned_results, brand_name)

    # 2. 计算品牌分数
    brand_scores = self._calculate_brand_scores(filled_results, brand_name, competitors)

    # 3. 计算 SOV
    sov_data = self._calculate_sov(filled_results, brand_name, competitors)

    # 4. 计算风险评分
    risk_data = self._calculate_risk(filled_results, brand_name)

    # 5. 品牌健康度
    health_data = self._calculate_brand_health(brand_scores.get(brand_name, {}))

    # 6. 生成洞察文本
    insights = self._generate_insights(brand_scores.get(brand_name, {}), brand_name)

    # 7. 计算首次提及率
    first_mention_data = self._calculate_first_mention_by_platform(filled_results)

    # 8. 计算拦截风险
    interception_risks = self._calculate_interception_risks(filled_results, brand_name)

    # 9. 整合报告
    report = {
        'brandName': brand_name,
        'competitors': competitors,
        'brandScores': brand_scores,
        'sov': sov_data,
        'risk': risk_data,
        'health': health_data,
        'insights': insights,
        'firstMentionByPlatform': first_mention_data,
        'interceptionRisks': interception_risks,
        'overallScore': brand_scores.get(brand_name, {}).get('overallScore', 50),
        'timestamp': datetime.now().isoformat()
    }

    return report
三、数据流向图
┌─────────────────────────────────────────────────────────────────────────────────┐
│                           品牌洞察报告完整数据流                                  │
└─────────────────────────────────────────────────────────────────────────────────┘

┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│   前端页面    │────▶│  云函数层    │────▶│  后端 API 层   │
│ report-v2.js │     │getDiagnosis  │     │diagnosis_api.│
│              │     │Report/index.js│    │py            │
└──────────────┘     └──────────────┘     └──────────────┘
                            │                     │
                            │                     ▼
                            │            ┌──────────────────┐
                            │            │diagnosis_report_ │
                            │            │service.py        │
                            │            │get_full_report() │
                            │            └──────────────────┘
                            │                     │
                            │                    ▼
                            │            ┌──────────────────┐
                            │            │Repository 层     │
                            │            │- ReportRepo      │
                            │            │- ResultRepo      │
                            │            │- AnalysisRepo    │
                            │            └──────────────────┘
                            │                     │
                            │                    ▼
                            │            ┌──────────────────┐
                            │            │SQLite 数据库      │
                            │            │- diagnosis_reports│
                            │            │- diagnosis_results│
                            │            │- diagnosis_analysis│
                            │            └──────────────────┘
                            │                     │
                            ▼                    ▼
                   ┌────────────────────────────────────────┐
                   │         报告数据服务层                   │
                   │  report_data_service.py                │
                   │  generate_full_report()                │
                   └────────────────────────────────────────┘
                                     │
                   ┌─────────────────┼─────────────────┐
                   │                 │                 │
                   ▼                 ▼                 ▼
          ┌──────────────┐  ┌──────────────┐  ┌──────────────┐
          │报告聚合器     │  │ROI 计算器     │  │行动计划生成器 │
          │ReportAgg     │  │ROICalculator │  │ActionPlanGen │
          └──────────────┘  └──────────────┘  └──────────────┘
                   │                 │                 │
                   └─────────────────┼─────────────────┘
                                     │
                                     ▼
                          ┌─────────────────────┐
                          │  完整报告数据结构    │
                          │ {                   │
                          │   report,           │
                          │   results,          │
                          │   analysis,         │
                          │   brandDistribution,│
                          │   sentimentDistribution,│
                          │   keywords,         │
                          │   validation,       │
                          │   qualityHints      │
                          │ }                   │
                          └─────────────────────┘
                                     │
                                     ▼
                          ┌─────────────────────┐
                          │  前端渲染展示        │
                          │ - 品牌分数卡片       │
                          │ - SOV 分析图         │
                          │ - 风险评分           │
                          │ - 品牌健康度         │
                          │ - 洞察文本           │
                          │ - 拦截风险列表       │
                          │ - 首次提及率         │
                          └─────────────────────┘
3.1 诊断执行流程 (7 个阶段)
┌─────────────────────────────────────────────────────────────────────────┐
│                        诊断执行 7 阶段流程                               │
└─────────────────────────────────────────────────────────────────────────┘

  阶段 1: 初始化          阶段 2: AI 调用          阶段 3: 结果保存
  ┌─────────────┐       ┌─────────────┐       ┌─────────────┐
  │ - 设置内存状态 │       │ - 并行 AI 调用  │       │ - 创建报告记录 │
  │ - 记录参数    │       │ - 收集结果   │       │ - 分批保存结果 │
  │ - 不写数据库  │──────▶│ - 网络 IO    │──────▶│ - 短事务提交  │
  └─────────────┘       └─────────────┘       └─────────────┘
         │                      │                      │
         │                      │                      ▼
         │                      │              ┌─────────────┐
         │                      │              │ 阶段 4: 结果验证 │
         │                      │              │ - 验证数量   │
         │                      │              │ - 验证质量   │
         │                      │              │ - 只读验证   │
         │                      │              └─────────────┘
         │                      │                      │
         │                      │                      ▼
         │                      │              ┌─────────────┐
         │                      │              │ 阶段 5: 后台分析 │
         │                      │              │ - 品牌分析   │
         │                      │              │ - 竞争分析   │
         │                      │              │ - 异步执行   │
         │                      │              └─────────────┘
         │                      │                      │
         │                      │                      ▼
         │                      │              ┌─────────────┐
         │                      │              │ 阶段 6: 报告聚合 │
         │                      │              │ - 聚合数据   │
         │                      │              │ - 计算指标   │
         │                      │              │ - 短事务提交  │
         │                      │              └─────────────┘
         │                      │                      │
         │                      │                      ▼
         │                      │              ┌─────────────┐
         │                      │              │ 阶段 7: 完成   │
         │                      │              │ - 更新状态   │
         │                      │              │ - 发送通知   │
         │                      │              │ - 短事务提交  │
         │                      │              └─────────────┘
3.2 数据库表结构
-- 核心表结构
diagnosis_reports       -- 报告主表
├── id                  -- 主键
├── execution_id        -- 执行 ID (唯一索引)
├── user_id             -- 用户 ID
├── brand_name          -- 品牌名称
├── competitor_brands   -- 竞品列表 (JSON)
├── selected_models     -- AI 模型列表 (JSON)
├── custom_questions    -- 自定义问题 (JSON)
├── status              -- 状态 (initializing/ai_fetching/analyzing/completed/failed)
├── progress            -- 进度 (0-100)
├── stage               -- 阶段 (init/ai_fetching/results_saving/...)
├── is_completed        -- 是否完成
├── created_at          -- 创建时间
└── completed_at        -- 完成时间

diagnosis_results       -- 诊断结果明细表
├── id                  -- 主键
├── report_id           -- 报告 ID (外键)
├── execution_id        -- 执行 ID
├── brand               -- 品牌名称
├── question            -- 问题文本
├── model               -- AI 模型名称
├── response_content    -- AI 响应内容
├── response_latency    -- 响应延迟
├── geo_data            -- GEO 分析数据 (JSON)
├── quality_score       -- 质量评分
├── quality_level       -- 质量等级
├── quality_details     -- 质量详情 (JSON)
├── status              -- 状态 (success/error)
├── error_message       -- 错误信息
└── created_at          -- 创建时间

diagnosis_analysis      -- 诊断分析数据表
├── id                  -- 主键
├── report_id           -- 报告 ID (外键)
├── execution_id        -- 执行 ID
├── analysis_type       -- 分析类型 (competitive_analysis/brand_scores/semantic_drift/...)
├── analysis_data       -- 分析数据 (JSON)
└── created_at          -- 创建时间
四、初步识别的问题点
4.1 高危问题 (P0)
问题编号
问题描述
相关文件
影响
P0-1
AI 返回空内容导致 NOT NULL 约束冲突
diagnosis_report_repository.py 第 520 行
诊断结果保存失败，前端无数据
P0-2
数据库连接泄漏
diagnosis_report_repository.py 上下文管理器
连接池耗尽，后续请求失败
P0-3
诊断结果未保存到 diagnosis_results 表
diagnosis_orchestrator.py 阶段 3
前端获取不到详细结果
P0-4
execution_store 内存泄漏
diagnosis_orchestrator.py
长时间运行后内存耗尽
P0-5
报告状态与 stage 不一致
diagnosis_report_repository.py
前端状态显示错误
P0-6
云函数 API_BASE_URL 配置错误
getDiagnosisReport/index.js 第 16 行
开发环境配置无法连接生产后端
P0-7
报告数据格式不统一 (snake_case vs camelCase)
多处转换逻辑
前端解析失败
4.2 中危问题 (P1)
问题编号
问题描述
相关文件
影响
P1-1
验证失败时缺少降级方案
diagnosis_report_service.py
用户看到空白页面
P1-2
分析数据缺失时未提供默认值
diagnosis_report_service.py 第 480 行
报告部分内容为空
P1-3
报告质量评分低时未提示用户
diagnosis_api.py
用户不知道数据质量问题
P1-4
前端错误处理不完善
reportService.js
错误提示不友好
P1-5
缓存策略不合理
reportService.js 缓存超时 5 分钟
数据更新延迟
P1-6
WebSocket 降级轮询逻辑复杂
report-v2.js
连接状态管理混乱
4.3 低危问题 (P2)
问题编号
问题描述
相关文件
影响
P2-1
日志记录过于详细
多处
日志文件过大
P2-2
错误码不统一
多处
错误追踪困难
P2-3
缺少数据质量监控指标
-
无法主动发现问题
P2-4
前端组件复用性差
report-v2.wxml
代码维护成本高
五、诊断规则与条件判断分析
5.1 诊断结果有效性判断
# 文件：diagnosis_report_service.py

# 场景 1: 报告状态为 failed
if report.get('status') == 'failed':
    return self._create_fallback_report(...)

# 场景 2: 报告处理超时 (超过 10 分钟)
if report.get('status') == 'processing':
    created_at = report.get('created_at')
    if created_at:
        created_time = datetime.fromisoformat(created_at)
        time_diff = (datetime.now() - created_time).total_seconds()
        if time_diff > 600:  # 超过 10 分钟
            return self._create_fallback_report(...)

# 场景 3: 结果为空
if not results or len(results) == 0:
    progress = report.get('progress', 0)
    if progress > 0 and progress < 100:
        return self._create_partial_fallback_report(...)
    return self._create_fallback_report(...)
5.2 数据质量验证规则
# 文件：diagnosis_report_service.py

def _validate_report_data(self, report: Dict, results: List, analysis: Dict):
    errors = []
    warnings = []

    # 1. 验证报告主数据
    if not report.get('execution_id'):
        errors.append('execution_id 缺失')
    if not report.get('brand_name'):
        errors.append('brand_name 缺失')

    # 2. 验证结果数据
    if not isinstance(results, list):
        errors.append('results 必须是数组')
    elif len(results) == 0:
        warnings.append('结果数组为空')

    # 3. 验证分析数据
    required_analysis = ['competitive_analysis', 'brand_scores']
    for analysis_type in required_analysis:
        if analysis_type not in analysis:
            warnings.append(f'缺少分析数据：{analysis_type}')

    # 4. 验证校验和
    checksum = report.get('checksum')
    if checksum:
        calculated_checksum = calculate_checksum({...})
        if checksum != calculated_checksum:
            errors.append('校验和不匹配，数据可能已损坏')

    return {
        'is_valid': len(errors) == 0,
        'errors': errors,
        'warnings': warnings
    }
5.3 情感分布计算规则
# 文件：diagnosis_report_service.py

def _calculate_sentiment_distribution(self, results: List[Dict[str, Any]]):
    sentiment_counts = {'positive': 0, 'neutral': 0, 'negative': 0}

    for result in results:
        if not result:
            continue

        geo_data = result.get('geo_data') or {}
        sentiment = geo_data.get('sentiment', 0) if geo_data else 0

        # 情感阈值判断
        if sentiment > 0.3:
            sentiment_counts['positive'] += 1
        elif sentiment < -0.3:
            sentiment_counts['negative'] += 1
        else:
            sentiment_counts['neutral'] += 1

    return {
        'data': sentiment_counts,
        'total_count': sum(sentiment_counts.values())
    }
六、关键数据路径追踪
6.1 "没有任何真实诊断信息" 问题追踪
当用户报告"没有任何真实诊断信息"时，数据可能在以下环节丢失：
1. AI 调用阶段 (diagnosis_orchestrator.py - _phase_ai_fetching)
   └─▶ 风险：AI 返回空结果或错误
   └─▶ 检查点：phase2_result.success

2. 结果保存阶段 (diagnosis_orchestrator.py - _phase_results_saving_transaction)
   └─▶ 风险：数据库保存失败或内容被过滤
   └─▶ 检查点：result_repo.add_batch() 返回值

3. 结果验证阶段 (diagnosis_orchestrator.py - _phase_results_validating)
   └─▶ 风险：验证通过但实际数据为空
   └─▶ 检查点：db_results 是否为空数组

4. 报告查询阶段 (diagnosis_api.py - get_full_report)
   └─▶ 风险：查询条件不匹配或数据被过滤
   └─▶ 检查点：result_repo.get_by_execution_id() 返回值

5. 前端处理阶段 (reportService.js - getFullReport)
   └─▶ 风险：数据格式转换错误或过滤条件过严
   └─▶ 检查点：_processReportData() 处理结果
6.2 关键日志检查点
# 1. AI 调用完成日志
api_logger.info(f"[Orchestrator] AI 调用完成：{execution_id}, 结果数={len(ai_results)}")

# 2. 结果保存完成日志
db_logger.info(f"[Orchestrator] 结果保存完成：{execution_id}, report_id={report_id}")

# 3. 验证完成日志
api_logger.info(f"[Orchestrator] 验证完成：{execution_id}, 数据库结果数={len(db_results)}")

# 4. API 查询日志
api_logger.info(f"获取完整报告：execution_id={execution_id}, results={len(report.get('results', []))}")
七、建议的修复优先级
第一阶段 (立即修复 - P0)
1.
修复 AI 空内容处理 - 在 diagnosis_report_repository.py 中添加空内容检测和占位符
2.
修复数据库连接泄漏 - 确保上下文管理器正确 yield 和清理
3.
确保结果保存到 diagnosis_results 表 - 验证阶段 3 的事务提交
4.
修复云函数 API 配置 - 使用环境变量配置 API_BASE_URL
第二阶段 (短期修复 - P1)
1.
完善降级方案 - 为各种失败场景提供友好的用户提示
2.
添加数据质量监控 - 记录质量指标并设置告警
3.
优化缓存策略 - 根据报告状态动态调整缓存时间
4.
统一数据格式 - 确保前后端数据格式一致
第三阶段 (中期优化 - P2)
1.
添加端到端测试 - 覆盖完整诊断流程
2.
性能优化 - 减少数据库查询和连接持有时间
3.
代码重构 - 提高组件复用性和可维护性
这份分析报告涵盖了品牌洞察报告模块的完整实现链路，包括所有相关文件、关键代码、数据流向和潜在问题点。建议按照修复优先级逐步解决识别出的问题。
我将全面分析"品牌洞察报告"模块的完整实现链路。让我开始系统性地搜索和分析相关代码。
现在让我详细阅读核心文件，从后端服务开始：
现在我已经完成了对"品牌洞察报告"模块的全面分析。让我整理一份详细的分析报告：
品牌洞察报告模块 - 完整实现链路分析报告
一、相关文件完整路径清单
1.1 后端核心服务层 (Backend Services)
文件路径
职责描述
/Users/sgl/PycharmProjects/PythonProject/backend_python/wechat_backend/services/report_data_service.py
报告数据聚合服务，负责从各数据源聚合完整报告数据
/Users/sgl/PycharmProjects/PythonProject/backend_python/wechat_backend/services/report_aggregator.py
报告聚合器，计算品牌分数、SOV、风险评分
/Users/sgl/PycharmProjects/PythonProject/backend_python/wechat_backend/services/diagnosis_orchestrator.py
诊断编排器，统一协调所有诊断子流程
/Users/sgl/PycharmProjects/PythonProject/backend_python/wechat_backend/services/report_generation_service.py
报告生成服务
/Users/sgl/PycharmProjects/PythonProject/backend_python/wechat_backend/diagnosis_report_service.py
诊断报告服务层，业务逻辑处理
/Users/sgl/PycharmProjects/PythonProject/backend_python/wechat_backend/diagnosis_report_repository.py
诊断报告仓库层，数据库 CRUD 操作
/Users/sgl/PycharmProjects/PythonProject/backend_python/wechat_backend/diagnosis_report_storage.py
诊断报告存储服务
/Users/sgl/PycharmProjects/PythonProject/backend_python/wechat_backend/diagnosis_report_transformer.py
诊断报告转换器
1.2 后端 API 视图层 (Backend Views/API)
文件路径
职责描述
/Users/sgl/PycharmProjects/PythonProject/backend_python/wechat_backend/views/diagnosis_api.py
诊断报告 API 路由 (/api/diagnosis/report/{execution_id})
/Users/sgl/PycharmProjects/PythonProject/backend_python/wechat_backend/views/diagnosis_views.py
诊断相关视图模块，包含品牌诊断执行逻辑
/Users/sgl/PycharmProjects/PythonProject/backend_python/wechat_backend/views/report_views.py
Report 相关视图模块
1.3 前端服务层 (Frontend Services)
文件路径
职责描述
/Users/sgl/PycharmProjects/PythonProject/miniprogram/services/reportService.js
前端报告数据服务，封装 API 调用
/Users/sgl/PycharmProjects/PythonProject/miniprogram/pages/report-v2/report-v2.js
报告页面 v2 逻辑
/Users/sgl/PycharmProjects/PythonProject/miniprogram/cloudfunctions/getDiagnosisReport/index.js
云函数：获取完整诊断报告
1.4 Node.js 服务层 (Node.js Services)
文件路径
职责描述
/Users/sgl/PycharmProjects/PythonProject/services/reportAggregator.js
报告聚合服务 (Node.js 版)
/Users/sgl/PycharmProjects/PythonProject/services/scoringService.js
战略评分服务，计算 SOV、风险评分、品牌健康度
/Users/sgl/PycharmProjects/PythonProject/services/streamingReportAggregator.js
流式报告聚合器
/Users/sgl/PycharmProjects/PythonProject/services/dataLoaderService.js
数据加载服务
1.5 数据库迁移文件 (Database Migrations)
文件路径
职责描述
/Users/sgl/PycharmProjects/PythonProject/backend_python/wechat_backend/database/migrations/002_enhanced_report_tables.sql
增强报告表结构迁移
/Users/sgl/PycharmProjects/PythonProject/backend_python/wechat_backend/database/migrations/003_fix_report_tables.sql
修复报告表结构迁移
二、关键代码片段分析
2.1 后端 API 入口 (/api/diagnosis/report/{execution_id})
文件: /Users/sgl/PycharmProjects/PythonProject/backend_python/wechat_backend/views/diagnosis_api.py
@diagnosis_bp.route('/report/<execution_id>', methods=['GET'])
@rate_limit(limit=60, window=60, per='user')
def get_full_report(execution_id):
    """获取完整诊断报告"""
    try:
        # 获取服务
        service = get_report_service()

        # 获取完整报告
        report = service.get_full_report(execution_id)

        # 处理报告不存在的情况
        if not report:
            return jsonify({
                'error': '报告不存在',
                'execution_id': execution_id,
                'suggestion': '请检查执行 ID 是否正确，或重新进行诊断'
            }), 404

        # 处理报告为字典但包含 error 字段的情况
        if isinstance(report, dict) and report.get('error'):
            return jsonify(report), 200

        # 验证报告并返回验证信息
        validation = get_validation_service().validate_report(report)
        report['validation'] = validation

        # 转换为 camelCase
        return jsonify(convert_response_to_camel(report)), 200

    except Exception as e:
        api_logger.error(f"获取完整报告失败：execution_id={execution_id}, 错误：{e}", exc_info=True)
        return jsonify({
            'error': '获取报告失败',
            'message': str(e),
            'execution_id': execution_id,
            'suggestion': '请稍后重试或联系技术支持'
        }), 500
2.2 报告服务层核心方法
文件: /Users/sgl/PycharmProjects/PythonProject/backend_python/wechat_backend/diagnosis_report_service.py
def get_full_report(self, execution_id: str) -> Optional[Dict[str, Any]]:
    """获取完整报告（P0-REPORT-2 优化：处理失败场景）"""
    db_logger.info(f"获取完整报告：{execution_id}")

    # 1. 获取报告主数据
    report = self.report_repo.get_by_execution_id(execution_id)
    if not report:
        return self._create_fallback_report(execution_id, '报告不存在', 'not_found')

    # 2. 获取结果明细
    results = self.result_repo.get_by_execution_id(execution_id)

    # P1-1 降级方案：结果为空时的处理
    if not results or len(results) == 0:
        return self._create_fallback_report(
            execution_id,
            '诊断已完成，但未生成有效结果',
            'no_results',
            report
        )

    # 3. 获取分析数据
    analysis = self.analysis_repo.get_by_execution_id(execution_id)
    analysis_data = self._ensure_complete_analysis(analysis, execution_id)

    # 4. 计算品牌分布
    brand_distribution = self._calculate_brand_distribution(results)

    # 5. 计算情感分布
    sentiment_distribution = self._calculate_sentiment_distribution(results)

    # 6. 提取关键词
    keywords = self._extract_keywords(results)

    # 7. 构建完整报告
    full_report = {
        'report': report,
        'results': results,
        'analysis': analysis_data,
        'brandDistribution': brand_distribution,
        'sentimentDistribution': sentiment_distribution,
        'keywords': keywords,
        'meta': {...},
        'validation': validation,
        'qualityHints': {...}
    }

    return convert_response_to_camel(full_report)
2.3 诊断编排器核心流程
文件: /Users/sgl/PycharmProjects/PythonProject/backend_python/wechat_backend/services/diagnosis_orchestrator.py
async def execute_diagnosis(self, user_id, brand_list, selected_models, custom_questions, ...) -> Dict[str, Any]:
    """执行完整诊断流程"""
    try:
        # ========== 阶段 1: 初始化 (事务外 - 仅内存) ==========
        phase1_result = await self._phase_init()
        if not phase1_result.success:
            raise ValueError(f"初始化失败：{phase1_result.error}")

        # ========== 阶段 2: AI 调用 (事务外 - 网络 IO) ==========
        phase2_result = await self._phase_ai_fetching(brand_list, selected_models, custom_questions, user_id, user_level)
        if not phase2_result.success:
            raise ValueError(f"AI 调用失败：{phase2_result.error}")

        # ========== 阶段 3: 结果保存 (事务 A - 短事务) ==========
        phase3_result = await self._phase_results_saving_transaction(phase2_result.data, brand_list, selected_models, custom_questions)
        if not phase3_result.success:
            raise ValueError(f"结果保存失败：{phase3_result.error}")

        # 等待数据库提交完成
        await asyncio.sleep(0.1)  # 等待 100ms，确保数据持久化

        # ========== 阶段 4: 结果验证 (事务外 - 只读验证) ==========
        phase4_result = await self._phase_results_validating(phase2_result.data)

        # ========== 阶段 5: 后台分析 (事务外 - 异步提交) ==========
        phase5_result = self._phase_background_analysis_async(db_results, brand_list)

        # ========== 阶段 6: 报告聚合 (事务 B - 短事务) ==========
        phase6_result = await self._phase_report_aggregating(db_results, brand_list)
        if not phase6_result.success:
            raise ValueError(f"报告聚合失败：{phase6_result.error}")

        # ========== 阶段 7: 完成 (事务 C - 状态更新) ==========
        phase7_result = await self._phase_complete(phase6_result.data)

        return {
            'success': True,
            'execution_id': self.execution_id,
            'report': phase6_result.data,
            'total_time': total_time,
            'trace_id': trace_id
        }

    except Exception as e:
        # 错误处理和日志记录
        return {
            'success': False,
            'execution_id': self.execution_id,
            'error': str(e),
            'error_code': error_code.value.code,
            'trace_id': trace_id
        }
2.4 前端报告服务
文件: /Users/sgl/PycharmProjects/PythonProject/miniprogram/services/reportService.js
async getFullReport(executionId) {
  try {
    console.log('[ReportService] Getting full report:', executionId);

    // 检查缓存
    const cached = this._getFromCache(executionId);
    if (cached) {
      return cached;
    }

    const res = await wx.cloud.callFunction({
      name: 'getDiagnosisReport',
      data: { executionId }
    });

    const report = res.result;

    // 处理空状态和错误状态
    if (!report) {
      return this._createEmptyReportWithSuggestion('报告不存在', 'not_found');
    }

    // 处理失败状态
    if (report.status === 'failed' || (report.success === false)) {
      return this._createFailedReportWithMetadata(report);
    }

    // 处理报告数据
    const processedReport = this._processReportData(report);

    // 缓存报告
    this._setCache(executionId, processedReport);

    return processedReport;
  } catch (error) {
    return this._createEmptyReportWithSuggestion(
      '获取报告失败：' + (error.message || '未知错误'),
      'error'
    );
  }
}
2.5 云函数入口
文件: /Users/sgl/PycharmProjects/PythonProject/miniprogram/cloudfunctions/getDiagnosisReport/index.js
exports.main = async (event, context) => {
  const { executionId } = event;

  if (!executionId) {
    return {
      success: false,
      error: '缺少执行 ID',
      suggestion: '请检查执行 ID 是否正确'
    };
  }

  try {
    const axios = require('axios');
    const response = await axios.get(`${API_BASE_URL}/api/diagnosis/report/${executionId}`, {
      timeout: 30000,
      headers: {
        'Content-Type': 'application/json',
        'User-Agent': 'WeChat-Miniprogram/1.0'
      }
    });

    const report = response.data;

    if (!report) {
      return createEmptyReport('报告不存在', 'not_found', executionId);
    }

    if (report.error) {
      return {
        success: false,
        ...report
      };
    }

    return {
      success: true,
      data: report
    };

  } catch (error) {
    if (error.code === 'ECONNREFUSED') {
      return createEmptyReport('后端服务不可连接', 'server_error', executionId);
    } else if (error.code === 'ETIMEDOUT') {
      return createEmptyReport('请求超时，请稍后重试', 'timeout', executionId);
    } else {
      return createEmptyReport('获取报告失败：' + (error.message || '未知错误'), 'error', executionId);
    }
  }
};
2.6 报告聚合器 (Python)
文件: /Users/sgl/PycharmProjects/PythonProject/backend_python/wechat_backend/services/report_aggregator.py
def aggregate(
    self,
    raw_results: List[Dict[str, Any]],
    brand_name: str,
    competitors: List[str],
    additional_data: Optional[Dict[str, Any]] = None
) -> Dict[str, Any]:
    """聚合品牌诊断报告"""
    # 1. 数据清洗
    cleaned_results = self._sanitize_results(raw_results)
    filled_results = self._fill_missing_data(cleaned_results, brand_name)

    # 2. 计算品牌分数
    brand_scores = self._calculate_brand_scores(filled_results, brand_name, competitors)

    # 3. 计算 SOV
    sov_data = self._calculate_sov(filled_results, brand_name, competitors)

    # 4. 计算风险评分
    risk_data = self._calculate_risk(filled_results, brand_name)

    # 5. 品牌健康度
    health_data = self._calculate_brand_health(brand_scores.get(brand_name, {}))

    # 6. 生成洞察文本
    insights = self._generate_insights(brand_scores.get(brand_name, {}), brand_name)

    # 7. 计算首次提及率
    first_mention_data = self._calculate_first_mention_by_platform(filled_results)

    # 8. 计算拦截风险
    interception_risks = self._calculate_interception_risks(filled_results, brand_name)

    # 9. 整合报告
    report = {
        'brandName': brand_name,
        'competitors': competitors,
        'brandScores': brand_scores,
        'sov': sov_data,
        'risk': risk_data,
        'health': health_data,
        'insights': insights,
        'firstMentionByPlatform': first_mention_data,
        'interceptionRisks': interception_risks,
        'overallScore': brand_scores.get(brand_name, {}).get('overallScore', 50),
        'timestamp': datetime.now().isoformat()
    }

    return report
三、数据流向图
┌─────────────────────────────────────────────────────────────────────────────────┐
│                           品牌洞察报告完整数据流                                  │
└─────────────────────────────────────────────────────────────────────────────────┘

┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│   前端页面    │────▶│  云函数层    │────▶│  后端 API 层   │
│ report-v2.js │     │getDiagnosis  │     │diagnosis_api.│
│              │     │Report/index.js│    │py            │
└──────────────┘     └──────────────┘     └──────────────┘
                            │                     │
                            │                     ▼
                            │            ┌──────────────────┐
                            │            │diagnosis_report_ │
                            │            │service.py        │
                            │            │get_full_report() │
                            │            └──────────────────┘
                            │                     │
                            │                    ▼
                            │            ┌──────────────────┐
                            │            │Repository 层     │
                            │            │- ReportRepo      │
                            │            │- ResultRepo      │
                            │            │- AnalysisRepo    │
                            │            └──────────────────┘
                            │                     │
                            │                    ▼
                            │            ┌──────────────────┐
                            │            │SQLite 数据库      │
                            │            │- diagnosis_reports│
                            │            │- diagnosis_results│
                            │            │- diagnosis_analysis│
                            │            └──────────────────┘
                            │                     │
                            ▼                    ▼
                   ┌────────────────────────────────────────┐
                   │         报告数据服务层                   │
                   │  report_data_service.py                │
                   │  generate_full_report()                │
                   └────────────────────────────────────────┘
                                     │
                   ┌─────────────────┼─────────────────┐
                   │                 │                 │
                   ▼                 ▼                 ▼
          ┌──────────────┐  ┌──────────────┐  ┌──────────────┐
          │报告聚合器     │  │ROI 计算器     │  │行动计划生成器 │
          │ReportAgg     │  │ROICalculator │  │ActionPlanGen │
          └──────────────┘  └──────────────┘  └──────────────┘
                   │                 │                 │
                   └─────────────────┼─────────────────┘
                                     │
                                     ▼
                          ┌─────────────────────┐
                          │  完整报告数据结构    │
                          │ {                   │
                          │   report,           │
                          │   results,          │
                          │   analysis,         │
                          │   brandDistribution,│
                          │   sentimentDistribution,│
                          │   keywords,         │
                          │   validation,       │
                          │   qualityHints      │
                          │ }                   │
                          └─────────────────────┘
                                     │
                                     ▼
                          ┌─────────────────────┐
                          │  前端渲染展示        │
                          │ - 品牌分数卡片       │
                          │ - SOV 分析图         │
                          │ - 风险评分           │
                          │ - 品牌健康度         │
                          │ - 洞察文本           │
                          │ - 拦截风险列表       │
                          │ - 首次提及率         │
                          └─────────────────────┘
3.1 诊断执行流程 (7 个阶段)
┌─────────────────────────────────────────────────────────────────────────┐
│                        诊断执行 7 阶段流程                               │
└─────────────────────────────────────────────────────────────────────────┘

  阶段 1: 初始化          阶段 2: AI 调用          阶段 3: 结果保存
  ┌─────────────┐       ┌─────────────┐       ┌─────────────┐
  │ - 设置内存状态 │       │ - 并行 AI 调用  │       │ - 创建报告记录 │
  │ - 记录参数    │       │ - 收集结果   │       │ - 分批保存结果 │
  │ - 不写数据库  │──────▶│ - 网络 IO    │──────▶│ - 短事务提交  │
  └─────────────┘       └─────────────┘       └─────────────┘
         │                      │                      │
         │                      │                      ▼
         │                      │              ┌─────────────┐
         │                      │              │ 阶段 4: 结果验证 │
         │                      │              │ - 验证数量   │
         │                      │              │ - 验证质量   │
         │                      │              │ - 只读验证   │
         │                      │              └─────────────┘
         │                      │                      │
         │                      │                      ▼
         │                      │              ┌─────────────┐
         │                      │              │ 阶段 5: 后台分析 │
         │                      │              │ - 品牌分析   │
         │                      │              │ - 竞争分析   │
         │                      │              │ - 异步执行   │
         │                      │              └─────────────┘
         │                      │                      │
         │                      │                      ▼
         │                      │              ┌─────────────┐
         │                      │              │ 阶段 6: 报告聚合 │
         │                      │              │ - 聚合数据   │
         │                      │              │ - 计算指标   │
         │                      │              │ - 短事务提交  │
         │                      │              └─────────────┘
         │                      │                      │
         │                      │                      ▼
         │                      │              ┌─────────────┐
         │                      │              │ 阶段 7: 完成   │
         │                      │              │ - 更新状态   │
         │                      │              │ - 发送通知   │
         │                      │              │ - 短事务提交  │
         │                      │              └─────────────┘
3.2 数据库表结构
-- 核心表结构
diagnosis_reports       -- 报告主表
├── id                  -- 主键
├── execution_id        -- 执行 ID (唯一索引)
├── user_id             -- 用户 ID
├── brand_name          -- 品牌名称
├── competitor_brands   -- 竞品列表 (JSON)
├── selected_models     -- AI 模型列表 (JSON)
├── custom_questions    -- 自定义问题 (JSON)
├── status              -- 状态 (initializing/ai_fetching/analyzing/completed/failed)
├── progress            -- 进度 (0-100)
├── stage               -- 阶段 (init/ai_fetching/results_saving/...)
├── is_completed        -- 是否完成
├── created_at          -- 创建时间
└── completed_at        -- 完成时间

diagnosis_results       -- 诊断结果明细表
├── id                  -- 主键
├── report_id           -- 报告 ID (外键)
├── execution_id        -- 执行 ID
├── brand               -- 品牌名称
├── question            -- 问题文本
├── model               -- AI 模型名称
├── response_content    -- AI 响应内容
├── response_latency    -- 响应延迟
├── geo_data            -- GEO 分析数据 (JSON)
├── quality_score       -- 质量评分
├── quality_level       -- 质量等级
├── quality_details     -- 质量详情 (JSON)
├── status              -- 状态 (success/error)
├── error_message       -- 错误信息
└── created_at          -- 创建时间

diagnosis_analysis      -- 诊断分析数据表
├── id                  -- 主键
├── report_id           -- 报告 ID (外键)
├── execution_id        -- 执行 ID
├── analysis_type       -- 分析类型 (competitive_analysis/brand_scores/semantic_drift/...)
├── analysis_data       -- 分析数据 (JSON)
└── created_at          -- 创建时间
四、初步识别的问题点
4.1 高危问题 (P0)
问题编号
问题描述
相关文件
影响
P0-1
AI 返回空内容导致 NOT NULL 约束冲突
diagnosis_report_repository.py 第 520 行
诊断结果保存失败，前端无数据
P0-2
数据库连接泄漏
diagnosis_report_repository.py 上下文管理器
连接池耗尽，后续请求失败
P0-3
诊断结果未保存到 diagnosis_results 表
diagnosis_orchestrator.py 阶段 3
前端获取不到详细结果
P0-4
execution_store 内存泄漏
diagnosis_orchestrator.py
长时间运行后内存耗尽
P0-5
报告状态与 stage 不一致
diagnosis_report_repository.py
前端状态显示错误
P0-6
云函数 API_BASE_URL 配置错误
getDiagnosisReport/index.js 第 16 行
开发环境配置无法连接生产后端
P0-7
报告数据格式不统一 (snake_case vs camelCase)
多处转换逻辑
前端解析失败
4.2 中危问题 (P1)
问题编号
问题描述
相关文件
影响
P1-1
验证失败时缺少降级方案
diagnosis_report_service.py
用户看到空白页面
P1-2
分析数据缺失时未提供默认值
diagnosis_report_service.py 第 480 行
报告部分内容为空
P1-3
报告质量评分低时未提示用户
diagnosis_api.py
用户不知道数据质量问题
P1-4
前端错误处理不完善
reportService.js
错误提示不友好
P1-5
缓存策略不合理
reportService.js 缓存超时 5 分钟
数据更新延迟
P1-6
WebSocket 降级轮询逻辑复杂
report-v2.js
连接状态管理混乱
4.3 低危问题 (P2)
问题编号
问题描述
相关文件
影响
P2-1
日志记录过于详细
多处
日志文件过大
P2-2
错误码不统一
多处
错误追踪困难
P2-3
缺少数据质量监控指标
-
无法主动发现问题
P2-4
前端组件复用性差
report-v2.wxml
代码维护成本高
五、诊断规则与条件判断分析
5.1 诊断结果有效性判断
# 文件：diagnosis_report_service.py

# 场景 1: 报告状态为 failed
if report.get('status') == 'failed':
    return self._create_fallback_report(...)

# 场景 2: 报告处理超时 (超过 10 分钟)
if report.get('status') == 'processing':
    created_at = report.get('created_at')
    if created_at:
        created_time = datetime.fromisoformat(created_at)
        time_diff = (datetime.now() - created_time).total_seconds()
        if time_diff > 600:  # 超过 10 分钟
            return self._create_fallback_report(...)

# 场景 3: 结果为空
if not results or len(results) == 0:
    progress = report.get('progress', 0)
    if progress > 0 and progress < 100:
        return self._create_partial_fallback_report(...)
    return self._create_fallback_report(...)
5.2 数据质量验证规则
# 文件：diagnosis_report_service.py

def _validate_report_data(self, report: Dict, results: List, analysis: Dict):
    errors = []
    warnings = []

    # 1. 验证报告主数据
    if not report.get('execution_id'):
        errors.append('execution_id 缺失')
    if not report.get('brand_name'):
        errors.append('brand_name 缺失')

    # 2. 验证结果数据
    if not isinstance(results, list):
        errors.append('results 必须是数组')
    elif len(results) == 0:
        warnings.append('结果数组为空')

    # 3. 验证分析数据
    required_analysis = ['competitive_analysis', 'brand_scores']
    for analysis_type in required_analysis:
        if analysis_type not in analysis:
            warnings.append(f'缺少分析数据：{analysis_type}')

    # 4. 验证校验和
    checksum = report.get('checksum')
    if checksum:
        calculated_checksum = calculate_checksum({...})
        if checksum != calculated_checksum:
            errors.append('校验和不匹配，数据可能已损坏')

    return {
        'is_valid': len(errors) == 0,
        'errors': errors,
        'warnings': warnings
    }
5.3 情感分布计算规则
# 文件：diagnosis_report_service.py

def _calculate_sentiment_distribution(self, results: List[Dict[str, Any]]):
    sentiment_counts = {'positive': 0, 'neutral': 0, 'negative': 0}

    for result in results:
        if not result:
            continue

        geo_data = result.get('geo_data') or {}
        sentiment = geo_data.get('sentiment', 0) if geo_data else 0

        # 情感阈值判断
        if sentiment > 0.3:
            sentiment_counts['positive'] += 1
        elif sentiment < -0.3:
            sentiment_counts['negative'] += 1
        else:
            sentiment_counts['neutral'] += 1

    return {
        'data': sentiment_counts,
        'total_count': sum(sentiment_counts.values())
    }
六、关键数据路径追踪
6.1 "没有任何真实诊断信息" 问题追踪
当用户报告"没有任何真实诊断信息"时，数据可能在以下环节丢失：
1. AI 调用阶段 (diagnosis_orchestrator.py - _phase_ai_fetching)
   └─▶ 风险：AI 返回空结果或错误
   └─▶ 检查点：phase2_result.success

2. 结果保存阶段 (diagnosis_orchestrator.py - _phase_results_saving_transaction)
   └─▶ 风险：数据库保存失败或内容被过滤
   └─▶ 检查点：result_repo.add_batch() 返回值

3. 结果验证阶段 (diagnosis_orchestrator.py - _phase_results_validating)
   └─▶ 风险：验证通过但实际数据为空
   └─▶ 检查点：db_results 是否为空数组

4. 报告查询阶段 (diagnosis_api.py - get_full_report)
   └─▶ 风险：查询条件不匹配或数据被过滤
   └─▶ 检查点：result_repo.get_by_execution_id() 返回值

5. 前端处理阶段 (reportService.js - getFullReport)
   └─▶ 风险：数据格式转换错误或过滤条件过严
   └─▶ 检查点：_processReportData() 处理结果
6.2 关键日志检查点
# 1. AI 调用完成日志
api_logger.info(f"[Orchestrator] AI 调用完成：{execution_id}, 结果数={len(ai_results)}")

# 2. 结果保存完成日志
db_logger.info(f"[Orchestrator] 结果保存完成：{execution_id}, report_id={report_id}")

# 3. 验证完成日志
api_logger.info(f"[Orchestrator] 验证完成：{execution_id}, 数据库结果数={len(db_results)}")

# 4. API 查询日志
api_logger.info(f"获取完整报告：execution_id={execution_id}, results={len(report.get('results', []))}")
七、建议的修复优先级
第一阶段 (立即修复 - P0)
1.
修复 AI 空内容处理 - 在 diagnosis_report_repository.py 中添加空内容检测和占位符
2.
修复数据库连接泄漏 - 确保上下文管理器正确 yield 和清理
3.
确保结果保存到 diagnosis_results 表 - 验证阶段 3 的事务提交
4.
修复云函数 API 配置 - 使用环境变量配置 API_BASE_URL
第二阶段 (短期修复 - P1)
1.
完善降级方案 - 为各种失败场景提供友好的用户提示
2.
添加数据质量监控 - 记录质量指标并设置告警
3.
优化缓存策略 - 根据报告状态动态调整缓存时间
4.
统一数据格式 - 确保前后端数据格式一致
第三阶段 (中期优化 - P2)
1.
添加端到端测试 - 覆盖完整诊断流程
2.
性能优化 - 减少数据库查询和连接持有时间
3.
代码重构 - 提高组件复用性和可维护性