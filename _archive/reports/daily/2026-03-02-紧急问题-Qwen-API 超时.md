# 2026-03-02-紧急问题：Qwen API 超时与前端数据不同步

**发现时间**: 2026-03-02 07:32  
**紧急程度**: 🔴 **高**  
**影响**: 诊断耗时 136 秒（正常 15-20 秒），前端无法感知完成状态

---

## 一、问题诊断

### 1.1 时间线分析

| 时间 | 事件 | 耗时 | 状态 |
|------|------|------|------|
| 07:29:52 | 任务创建，异步线程启动 | - | init/0% |
| 07:29:52 | Doubao 模型调用开始 | - | ai_fetching |
| 07:30:18 | Doubao 调用成功 | **26 秒** | ✅ |
| 07:30:18 | Qwen 模型调用开始 | - | ai_fetching |
| 07:32:08 | Qwen 调用成功 | **110 秒** ❌ | ✅ |
| 07:32:08 | 执行完成，数据库更新 | - | completed/100% |
| 07:32:08+ | 前端仍显示 init/0% | - | ❌ **问题** |

### 1.2 核心问题

**问题 1: Qwen API 严重超时**
- 正常耗时：15-20 秒
- 实际耗时：**110 秒**
- 原因：API 配额限制/网络问题/响应过长

**问题 2: 前端数据不同步**
- 后端：07:32:08 已完成，数据库 `stage=completed, progress=100`
- 前端：一直显示 `stage=init, progress=0%`
- 可能原因：
  1. 前端缓存了旧响应
  2. 轮询请求被拦截（Service Worker?）
  3. 前端未正确解析响应

**问题 3: 日志不匹配**
- 前端日志：显示 40 次 + 轮询
- 后端日志：只有 2 次查询记录（07:29:52）
- 说明：前端轮询请求**未到达后端**

---

## 二、立即行动

### 2.1 清除前端缓存（必须！）

```javascript
// 微信开发者工具
// 1. 工具 → 清除缓存 → 勾选"清除全部缓存"
// 2. 重新编译小程序
```

### 2.2 检查 Qwen API 状态

```bash
# 检查 API Key 配额
curl -X POST https://dashscope.aliyuncs.com/api/v1/services/aigc/text-generation/generation \
  -H "Authorization: Bearer $QWEN_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"model":"qwen-max","input":{"messages":[{"role":"user","content":"test"}]}}'

# 检查响应时间
time curl -X POST ...
```

### 2.3 检查 Service Worker 缓存

```javascript
// 在小程序中检查是否有 Service Worker 拦截
// miniprogram/app.js 或 app.json
```

---

## 三、后端修复建议

### 3.1 添加 API 超时保护

**文件**: `backend_python/wechat_backend/ai_timeout.py`

```python
# 确保 Qwen API 有超时配置
TIMEOUT_CONFIG = {
    'qwen': 30,  # 30 秒超时
    'doubao': 30,
    'deepseek': 30
}
```

### 3.2 添加状态查询日志

**文件**: `backend_python/wechat_backend/views/diagnosis_views.py`

```python
@wechat_bp.route('/test/status/<task_id>', methods=['GET'])
def get_task_status_api(task_id):
    api_logger.info(f"[TaskStatus] 收到查询请求：{task_id}")  # 添加这行
    # ... 现有逻辑
```

### 3.3 强制刷新响应头

```python
response = jsonify(response_data)
response.headers['Cache-Control'] = 'no-cache, no-store, must-revalidate'
response.headers['Pragma'] = 'no-cache'
response.headers['Expires'] = '0'
return response, 200
```

---

## 四、前端修复建议

### 4.1 添加缓存控制

**文件**: `miniprogram/services/taskStatusService.js`

```javascript
// 添加请求头防止缓存
wx.request({
  url: `${baseUrl}/test/status/${executionId}`,
  method: 'GET',
  header: {
    'Cache-Control': 'no-cache',
    'Pragma': 'no-cache',
    'Expires': '0'
  },
  // ...
})
```

### 4.2 添加响应日志

```javascript
// taskStatusService.js
getTaskStatus(executionId) {
  return new Promise((resolve, reject) => {
    wx.request({
      url: `${baseUrl}/test/status/${executionId}`,
      success: (res) => {
        console.log('[TaskStatus] 原始响应:', res.data)  // 添加这行
        const status = this.parseTaskStatus(res.data)
        console.log('[TaskStatus] 解析后状态:', status)  // 添加这行
        resolve(status)
      },
      fail: (err) => reject(err)
    })
  })
}
```

---

## 五、验证步骤

### 5.1 清除缓存后重试

```
1. 微信开发者工具 → 清除缓存
2. 重新编译小程序
3. 发起新的诊断请求
4. 观察：
   - Qwen API 耗时是否 <30 秒
   - 前端进度是否实时更新
   - 完成后是否自动跳转
```

### 5.2 检查后端日志

```bash
# 实时监控
tail -f logs/app.log | grep -E "Qwen|TaskStatus|并行引擎"

# 期望输出：
# [SingleModel] 调用模型 qwen
# [SingleModel] ✅ 模型 qwen 调用成功 (15-20 秒后)
# [NxM-Parallel] ✅ 执行完成 - 耗时=15-20 秒
# [TaskStatus] 收到查询请求：{id}
# [TaskStatus] 返回前端数据：{id}, stage=ai_fetching, progress=50
```

---

## 六、长期优化

### 6.1 Qwen API 降级策略

```python
# 如果 Qwen 超时，自动降级到其他模型
if model_name == 'qwen' and timeout > 30:
    logger.warning("Qwen 超时，降级到 Doubao")
    return await execute_with_model('doubao', prompt)
```

### 6.2 WebSocket 实时推送

```
当前：HTTP 轮询（易被缓存）
目标：WebSocket（实时，无缓存问题）

进度：
✅ 后端 WebSocket 服务器已实现
✅ 前端 WebSocket 客户端已实现
⏳ 需要启用（修改 featureFlags.js）
```

---

**报告人**: 系统架构组  
**时间**: 2026-03-02 07:35  
**状态**: 🔴 待处理
