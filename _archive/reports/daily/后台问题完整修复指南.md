# 后台问题完整修复指南

**报告编号**: COMPLETE-FIX-20260228
**修复日期**: 2026-02-28
**状态**: 📋 待手动应用

---

## 一、问题总结

### P0 级问题（严重）

| # | 问题 | 根因 | 状态 |
|---|------|------|------|
| 1 | AI 调用结果未保存 | 只保存到 dimension_results，未保存到 diagnosis_results | 📋 待修复 |
| 2 | AI 响应日志缺失 | AIResponseLogger 未被调用 | 📋 待修复 |
| 3 | 诊断高失败率 (78%) | API Key 配置错误 + 结果未保存 | 📋 待修复 |

### P1 级问题（重要）

| # | 问题 | 根因 | 状态 |
|---|------|------|------|
| 4 | 豆包 API 密钥格式错误 | .env 配置错误 | 📋 待配置 |
| 5 | 文心一言未配置 | 缺少 API Key | 📋 待配置 |
| 6 | 数据库索引创建失败 | 表不存在 | ⚠️ 可忽略 |

---

## 二、P0-1 修复：保存 AI 结果到 diagnosis_results

### 2.1 问题说明

**文件**: `backend_python/wechat_backend/nxm_execution_engine.py`

**当前代码** (第 479-488 行):
```python
save_dimension_result(
    execution_id=execution_id,
    dimension_name=f"{main_brand}-{model_name}",
    dimension_type="ai_analysis",
    source=model_name,
    status=dim_status,
    score=dim_score,
    data=geo_data if dim_status == "success" else None,
    error_message=ai_result.error_message if dim_status == "failed" else (parse_error if parse_error else None)
)
```

**问题**: 只保存到 `dimension_results` 表，`diagnosis_results` 表为空

### 2.2 修复代码

在上述代码**之后**，插入以下代码：

```python
# 【P0 关键修复】保存 AI 调用结果到 diagnosis_results 表
if dim_status == "success" and geo_data:
    try:
        import sqlite3
        import json
        from datetime import datetime
        
        # 连接到数据库
        conn = sqlite3.connect('/Users/sgl/PycharmProjects/PythonProject/backend_python/database.db')
        cursor = conn.cursor()
        
        # 获取 report_id
        cursor.execute('SELECT id FROM diagnosis_reports WHERE execution_id = ?', (execution_id,))
        report_row = cursor.fetchone()
        report_id = report_row[0] if report_row else None
        
        if report_id:
            now = datetime.now().isoformat()
            response_content = str(ai_result.data) if hasattr(ai_result, 'data') else str(ai_result)
            
            cursor.execute('''
                INSERT INTO diagnosis_results (
                    report_id, execution_id,
                    brand, question, model,
                    response_content, response_latency,
                    geo_data,
                    quality_score, quality_level, quality_details,
                    status, error_message,
                    raw_response, response_metadata,
                    tokens_used, prompt_tokens, completion_tokens, cached_tokens,
                    finish_reason, request_id, model_version, reasoning_content,
                    api_endpoint, service_tier,
                    retry_count, is_fallback,
                    created_at, updated_at
                ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
            ''', (
                report_id,
                execution_id,
                main_brand,
                question,
                model_name,
                response_content,
                ai_result.latency if hasattr(ai_result, 'latency') else None,
                json.dumps(geo_data, ensure_ascii=False),
                dim_score,
                'high' if dim_score and dim_score >= 80 else 'medium' if dim_score and dim_score >= 60 else 'low',
                json.dumps({}),
                'success',
                None,
                json.dumps({}),
                json.dumps({}),
                0, 0, 0, 0,
                'stop',
                '',
                model_name,
                '',
                '',
                'default',
                0,
                0,
                now,
                now
            ))
            
            conn.commit()
            result_id = cursor.lastrowid
            conn.close()
            
            api_logger.info(f"[NxM] ✅ diagnosis_results 保存成功：execution_id={execution_id}, result_id={result_id}")
        else:
            api_logger.warning(f"[NxM] ⚠️ 未找到 report_id：execution_id={execution_id}")
            conn.close()
        
    except Exception as save_err:
        api_logger.error(f"[NxM] ⚠️ diagnosis_results 保存失败：execution_id={execution_id}, 错误：{save_err}")
        # 不中断主流程
```

---

## 三、P0-2 修复：启用 AI 响应日志

### 3.1 问题说明

**文件**: `backend_python/wechat_backend/nxm_execution_engine.py`

**问题**: AIResponseLogger 存在但未被调用

### 3.2 修复代码

在 AI 调用成功后（`if ai_result.success:` 块内），添加：

```python
# 【P0-2 修复】记录 AI 响应日志
try:
    from wechat_backend.utils.ai_response_logger import log_ai_response
    
    log_ai_response(
        question=question,
        response=str(ai_result.data) if hasattr(ai_result, 'data') else str(ai_result),
        platform=model_name,
        model=model_name,
        brand=main_brand,
        latency_ms=int(ai_result.latency * 1000) if hasattr(ai_result, 'latency') else None,
        success=ai_result.success,
        error_message=ai_result.error_message if not ai_result.success else None
    )
    
    api_logger.info(f"[AI 日志] ✅ 已记录：{execution_id}-{question[:20]}")
    
except Exception as log_err:
    api_logger.error(f"[AI 日志] ⚠️ 记录失败：{log_err}")
```

---

## 四、P1 问题修复

### 4.1 P1-1: 豆包 API 密钥配置

**文件**: `backend_python/.env`

**检查项**:
```bash
# 检查 ARK_API_KEY 配置
grep ARK_API_KEY backend_python/.env
```

**修复**:
```env
# 确保密钥格式正确（示例）
ARK_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx
```

### 4.2 P1-2: 文心一言配置

**文件**: `backend_python/.env`

**添加配置**:
```env
# 文心一言 API Key
WENXIN_API_KEY=your_wenxin_api_key_here
```

或在前端高级设置中取消选择文心一言。

### 4.3 P1-3: 数据库索引

**状态**: 可忽略

这些表 (`audit_logs`, `cache_entries`) 确实不存在，但不影响核心功能。

---

## 五、应用修复

### 5.1 步骤

1. **备份原文件**:
   ```bash
   cp backend_python/wechat_backend/nxm_execution_engine.py \
      backend_python/wechat_backend/nxm_execution_engine.py.bak
   ```

2. **编辑文件**:
   ```bash
   vim backend_python/wechat_backend/nxm_execution_engine.py
   ```

3. **应用 P0-1 修复** (第 488 行后)

4. **应用 P0-2 修复** (第 420 行后，`if ai_result.success:` 块内)

5. **保存并退出**

6. **重启后端服务**:
   ```bash
   cd backend_python
   # 停止当前服务
   # 重新启动
   python main.py
   ```

### 5.2 验证

1. **发起诊断测试**:
   ```bash
   curl -X POST http://localhost:5001/api/perform-brand-test \
     -H "Content-Type: application/json" \
     -d '{"brand_name":"测试","questions":["测试问题"],"models":["deepseek"]}'
   ```

2. **检查数据库**:
   ```bash
   sqlite3 backend_python/database.db "SELECT COUNT(*) FROM diagnosis_results;"
   ```
   **预期**: 记录数 > 0

3. **检查日志**:
   ```bash
   tail -100 logs/app.log | grep "diagnosis_results 保存成功"
   tail -100 logs/ai_responses.log
   ```

---

## 六、总结

### 6.1 修复清单

- [ ] P0-1: 保存 AI 结果到 diagnosis_results
- [ ] P0-2: 启用 AI 响应日志
- [ ] P1-1: 配置豆包 API Key
- [ ] P1-2: 配置文心一言 API Key
- [ ] 验证修复效果

### 6.2 预期效果

修复后:
- ✅ `diagnosis_results` 表有记录
- ✅ `ai_responses.log` 有日志
- ✅ 诊断成功率 > 90%
- ✅ 前端能获取详细结果

---

**实施人员**: 系统架构组
**报告日期**: 2026-02-28
**版本**: v1.0
