# 品牌诊断系统 - 代码改造方案

**文档编号**: PLAN-20260225-001  
**编制人**: 首席架构师 张工  
**日期**: 2026-02-25  
**版本**: 1.0  
**状态**: 待评审  

---

## 一、现有代码审计报告

### 1.1 代码架构总览

```
backend_python/
├── wechat_backend/
│   ├── views/
│   │   └── diagnosis_views.py        # 报告生成入口 (2665 行)
│   ├── nxm_execution_engine.py       # 执行引擎 (800 行)
│   ├── nxm_scheduler.py              # 任务调度 (161 行)
│   ├── nxm_result_aggregator.py      # 结果聚合 (300 行)
│   ├── ai_adapters/
│   │   ├── base_adapter.py           # 基础适配器
│   │   ├── doubao_adapter.py         # 豆包适配器
│   │   └── doubao_priority_adapter.py # 豆包优先级适配器
│   ├── fault_tolerant_executor.py    # 容错执行器 (新建)
│   ├── repositories/                 # 数据仓库 (新建)
│   │   ├── report_snapshot_repository.py
│   │   └── dimension_result_repository.py
│   ├── diagnosis_report_repository.py  # 诊断报告仓库 (旧)
│   └── diagnosis_report_storage.py     # 诊断报告存储 (旧)
└── tests/
    └── test_brand_diagnosis_system.py  # 综合测试 (新建)
```

### 1.2 核心流程分析

#### 报告生成入口
**文件**: `diagnosis_views.py:91-370`  
**函数**: `perform_brand_test()`

```python
# 当前实现（简化）
@wechat_bp.route('/api/perform-brand-test', methods=['POST'])
def perform_brand_test():
    # 1. 参数验证
    data = request.get_json()
    brand_list = data.get('brandList', [])
    selected_models = data.get('selectedModels', [])
    custom_questions = data.get('customQuestions', [])
    
    # 2. 生成 execution_id
    execution_id = str(uuid.uuid4())
    
    # 3. 初始化内存存储
    execution_store[execution_id] = {
        'progress': 0,
        'status': 'initializing',
        'results': []
    }
    
    # 4. 启动异步线程
    thread = Thread(target=run_async_test)
    thread.start()
    
    # 5. 立即返回
    return jsonify({'status': 'success', 'execution_id': execution_id})
```

**问题识别**:
| 问题 | 影响 | 严重度 |
|------|------|--------|
| execution_store 纯内存存储 | 服务重启数据丢失 | 🔴 高 |
| 未创建 diagnosis_reports 记录 | 进度无法持久化 | 🔴 高 |
| 异步线程无异常处理 | 失败无日志 | 🟡 中 |

#### 执行引擎
**文件**: `nxm_execution_engine.py:100-200`  
**函数**: `execute_nxm_test()`

```python
# 当前实现（简化）
def execute_nxm_test(execution_id, main_brand, competitor_brands, ...):
    scheduler = create_scheduler(execution_id, execution_store)
    
    # 遍历品牌 × 问题 × 模型
    for brand in all_brands:
        for q_idx, question in enumerate(raw_questions):
            for model_info in selected_models:
                # 创建 AI 客户端
                client = AIAdapterFactory.create(model_name)
                
                # 同步调用 AI（有问题）
                loop = asyncio.new_event_loop()
                response = loop.run_until_complete(
                    asyncio.wait_for(
                        loop.run_in_executor(None, lambda: client.generate_response(...)),
                        timeout=timeout
                    )
                )
                
                # 解析 GEO 数据
                geo_data, parse_error = parse_geo_with_validation(response, ...)
                
                # 添加结果
                results.append(geo_data)
```

**问题识别**:
| 问题 | 影响 | 严重度 |
|------|------|--------|
| `generate_response` 方法不存在 | AI 调用 100% 失败 | 🔴 致命 |
| 无 try-catch 包裹 | 单个失败中断整体 | 🔴 高 |
| 结果未持久化 | 进度查询失效 | 🔴 高 |
| 未使用 FaultTolerantExecutor | 无容错机制 | 🟡 中 |

#### 数据存储
**文件**: `diagnosis_report_repository.py`  
**函数**: `save_test_record()`

```python
# 当前实现
def save_test_record(user_openid, brand_name, ...):
    with get_db_connection() as conn:
        cursor = conn.cursor()
        cursor.execute('''
            INSERT INTO test_records (...)
            VALUES (?, ?, ?, ...)
        ''', (...))
        return cursor.lastrowid
```

**问题识别**:
| 问题 | 影响 | 严重度 |
|------|------|--------|
| 只存 test_records 表 | 无快照存储 | 🔴 高 |
| 无 report_snapshots 表 | 历史一致性无保障 | 🔴 高 |
| 无 dimension_results 表 | 维度结果丢失 | 🟡 中 |

### 1.3 脆弱点标记

```
┌─────────────────────────────────────────────────────────────┐
│                    现有代码脆弱点地图                        │
└─────────────────────────────────────────────────────────────┘

[1] diagnosis_views.py:272 - execution_store 纯内存
    │ ❌ 服务重启数据丢失
    ▼
[2] diagnosis_views.py:316 - execute_nxm_test 调用
    │ ❌ 无持久化
    ▼
[3] nxm_execution_engine.py:174 - client.generate_response()
    │ ❌ 方法不存在，100% 失败
    │ ❌ 无 try-catch
    ▼
[4] nxm_execution_engine.py:195 - parse_geo_with_validation
    │ ⚠️ 解析失败无降级
    ▼
[5] nxm_execution_engine.py:240 - results.append
    │ ❌ 仅内存存储，未写数据库
    ▼
[6] 无 snapshot 保存逻辑
    │ ❌ 历史报告无快照
```

---

## 二、改造方案设计

### 2.1 改造原则

1. **精准手术** - 只修改必要代码，保留可用部分
2. **渐进式改造** - 分阶段实施，每步可回退
3. **向后兼容** - 兼容旧数据结构
4. **零宕机** - 改造过程不影响现有用户

### 2.2 改造点清单

| 编号 | 改造点 | 文件 | 行号 | 改造方式 | 风险等级 |
|------|--------|------|------|---------|---------|
| M001 | 修复 AI 调用方法 | nxm_execution_engine.py | 174 | generate_response→send_prompt | 🟢 低 |
| M002 | 添加容错包裹 | nxm_execution_engine.py | 160-200 | 引入 FaultTolerantExecutor | 🟡 中 |
| M003 | 实时持久化 | nxm_execution_engine.py | 240 | 添加 save_dimension_result | 🟡 中 |
| M004 | 快照存储 | diagnosis_views.py | 370 | 添加 save_report_snapshot | 🟡 中 |
| M005 | 数据库表创建 | repositories/ | - | 执行迁移脚本 | 🟢 低 |
| M006 | 前端错误 UI | 小程序端 | - | 新增错误展示组件 | 🟡 中 |

### 2.3 详细改造方案

#### 【M001】修复 AI 调用方法

**文件**: `wechat_backend/nxm_execution_engine.py`  
**行号**: 174  
**改造前**:
```python
response = loop.run_until_complete(
    asyncio.wait_for(
        asyncio.get_event_loop().run_in_executor(
            None,
            lambda: client.generate_response(prompt=prompt, api_key=api_key)
        ),
        timeout=timeout
    )
)
```

**改造后**:
```python
# 修复：使用 send_prompt 方法而非 generate_response
response = loop.run_until_complete(
    asyncio.wait_for(
        asyncio.get_event_loop().run_in_executor(
            None,
            lambda: client.send_prompt(prompt=prompt)  # ✅ 修改
        ),
        timeout=timeout
    )
)
```

**影响范围**: 所有 AI 调用  
**测试验证**: 单元测试验证 send_prompt 调用

---

#### 【M002】添加容错包裹

**文件**: `wechat_backend/nxm_execution_engine.py`  
**行号**: 160-200  
**改造前**:
```python
try:
    # 同步调用 AI
    loop = asyncio.new_event_loop()
    response = loop.run_until_complete(...)
    
    # 解析 GEO 数据
    geo_data, parse_error = parse_geo_with_validation(response, ...)
    
except Exception as e:
    # 异常直接抛出，中断流程
    raise
```

**改造后**:
```python
# 引入容错执行器
from wechat_backend.fault_tolerant_executor import FaultTolerantExecutor

ft_executor = FaultTolerantExecutor(timeout_seconds=timeout)

# 使用容错包裹
result = await ft_executor.execute_with_fallback(
    task_func=lambda: client.send_prompt(prompt=prompt),
    task_name=f"{brand}-{model_name}",
    source=model_name
)

if result.status == "success":
    geo_data, parse_error = parse_geo_with_validation(result.data, ...)
    results.append(geo_data)
else:
    # ✅ 失败不中断，记录错误继续下一个
    results.append({
        "brand": brand,
        "model": model_name,
        "status": "failed",
        "error_message": result.error_message,
        "data": None
    })
```

**影响范围**: NxM 执行流程  
**测试验证**: 模拟 AI 失败，验证流程继续

---

#### 【M003】实时持久化

**文件**: `wechat_backend/nxm_execution_engine.py`  
**行号**: 240  
**改造前**:
```python
# 仅添加到内存
results.append(geo_data)
scheduler.update_progress(completed, total_tasks, 'ai_fetching')
```

**改造后**:
```python
from wechat_backend.repositories import save_dimension_result, save_task_status

# 添加到内存
results.append(geo_data)

# ✅ 实时持久化维度结果
save_dimension_result(
    execution_id=execution_id,
    dimension_name=f"{brand}-{model_name}",
    dimension_type="ai_analysis",
    source=model_name,
    status=geo_data.get("status", "success"),
    score=geo_data.get("rank"),
    data=geo_data,
    error_message=geo_data.get("_error")
)

# ✅ 实时更新进度
save_task_status(
    task_id=execution_id,
    stage='ai_fetching',
    progress=int((completed / total_tasks) * 100),
    status_text=f'已完成 {completed}/{total_tasks}'
)

scheduler.update_progress(completed, total_tasks, 'ai_fetching')
```

**影响范围**: 结果存储  
**测试验证**: 验证数据库有记录

---

#### 【M004】快照存储

**文件**: `wechat_python/views/diagnosis_views.py`  
**行号**: 370（run_async_test 函数末尾）  
**改造前**:
```python
def run_async_test():
    try:
        # 执行 NxM 测试
        result = execute_nxm_test(...)
        
        # 无快照保存
    except Exception as e:
        execution_store[execution_id]['status'] = 'failed'
```

**改造后**:
```python
from wechat_backend.repositories import save_report_snapshot

def run_async_test():
    try:
        # 执行 NxM 测试
        result = execute_nxm_test(...)
        
        # ✅ 构建完整报告
        report_data = {
            "reportId": execution_id,
            "userId": user_id,
            "brandName": main_brand,
            "generateTime": datetime.now().isoformat(),
            "reportVersion": "v2.0",
            "reportData": {
                "overallScore": calculate_overall_score(result),
                "overallStatus": result.get('status', 'completed'),
                "dimensions": result.get('results', [])
            }
        }
        
        # ✅ 保存快照
        save_report_snapshot(
            execution_id=execution_id,
            user_id=user_id,
            report_data=report_data
        )
        
        # ✅ 更新 diagnosis_reports 状态
        update_diagnosis_status(execution_id, 'completed')
        
    except Exception as e:
        execution_store[execution_id]['status'] = 'failed'
        # ✅ 即使失败也保存错误报告
        save_report_snapshot(
            execution_id=execution_id,
            user_id=user_id,
            report_data={"error": str(e), "status": "failed"}
        )
```

**影响范围**: 报告完成处理  
**测试验证**: 验证快照表有记录

---

#### 【M005】数据库表创建

**文件**: `backend_python/database.db`  
**操作**: 执行迁移脚本

```sql
-- 创建 report_snapshots 表
CREATE TABLE IF NOT EXISTS report_snapshots (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    execution_id TEXT UNIQUE NOT NULL,
    user_id TEXT NOT NULL,
    report_data TEXT NOT NULL,
    report_hash TEXT NOT NULL,
    size_kb INTEGER NOT NULL,
    storage_timestamp TEXT NOT NULL,
    report_version TEXT DEFAULT 'v1.0'
);

-- 创建 dimension_results 表
CREATE TABLE IF NOT EXISTS dimension_results (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    execution_id TEXT NOT NULL,
    dimension_name TEXT NOT NULL,
    dimension_type TEXT NOT NULL,
    source TEXT NOT NULL,
    status TEXT NOT NULL,
    score REAL,
    data TEXT,
    error_message TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 创建索引
CREATE INDEX IF NOT EXISTS idx_snapshot_execution_id ON report_snapshots(execution_id);
CREATE INDEX IF NOT EXISTS idx_snapshot_user_id ON report_snapshots(user_id);
CREATE INDEX IF NOT EXISTS idx_dimension_execution_id ON dimension_results(execution_id);
```

**影响范围**: 数据库结构  
**测试验证**: 验证表存在

---

#### 【M006】前端错误 UI

**文件**: 小程序端 `pages/report-detail/index.wxml`  
**改造前**:
```html
<view class="dimension-card">
  <text>{{dimension.name}}</text>
  <text>得分：{{dimension.score}}</text>
</view>
```

**改造后**:
```html
<view class="dimension-card {{dimension.status === 'failed' ? 'failed' : ''}}">
  <view class="dimension-header">
    <text class="dimension-name">{{dimension.name}}</text>
    <text wx:if="{{dimension.status === 'failed'}}" class="badge-warning">数据暂缺</text>
  </view>
  
  <view wx:if="{{dimension.status === 'success'}}">
    <text>得分：{{dimension.score}}</text>
    <!-- 原有内容 -->
  </view>
  
  <view wx:else class="error-box">
    <text class="error-icon">⚠️</text>
    <text class="error-message">{{dimension.error_message}}</text>
    <button class="retry-btn" bindtap="retryDimension" data-dimension="{{dimension}}">重新获取</button>
  </view>
</view>
```

**影响范围**: 前端展示  
**测试验证**: UI 验收

---

## 三、代码复用率评估

| 模块 | 复用方式 | 复用率 | 说明 |
|------|---------|--------|------|
| diagnosis_views.py | 保留入口，修改内部逻辑 | 70% | 路由和参数验证保留 |
| nxm_execution_engine.py | 保留框架，修改调用方式 | 50% | 循环结构保留，AI 调用修改 |
| nxm_scheduler.py | 完全保留 | 100% | 调度逻辑无需修改 |
| nxm_result_aggregator.py | 保留解析逻辑 | 80% | 添加错误处理 |
| ai_adapters/* | 完全保留 | 100% | 适配器无需修改 |
| fault_tolerant_executor.py | 新增 | 0% | 新增容错模块 |
| repositories/* | 新增 | 0% | 新增数据仓库 |

**总体复用率**: ~70%

---

## 四、风险与缓解

| 风险 | 影响 | 概率 | 缓解措施 |
|------|------|------|---------|
| M001 修改影响其他适配器 | 其他平台调用失败 | 低 | 单元测试全覆盖 |
| M002 容错包裹性能下降 | 执行时间增加 | 中 | 压测验证 |
| M003 数据库写入频繁 | 数据库性能瓶颈 | 中 | 添加批量写入优化 |
| M004 快照存储失败 | 报告无法查询 | 低 | 降级到文件存储 |
| M005 表创建冲突 | 数据库迁移失败 | 低 | IF NOT EXISTS 保护 |
| M006 前端兼容性问题 | 旧版小程序报错 | 中 | 条件渲染保护 |

---

## 五、实施计划

| 阶段 | 任务 | 负责人 | 预计时间 | 交付物 |
|------|------|--------|---------|--------|
| **Day 1** | 代码审计 | 张工 | 4 小时 | 本文档 |
| **Day 1** | 方案评审 | 全体 | 2 小时 | 评审意见 |
| **Day 2** | M001-M003 实施 | 李工 | 6 小时 | 修改后代码 |
| **Day 2** | M004-M005 实施 | 李工 | 4 小时 | 数据库迁移 |
| **Day 3** | M006 实施 | 王工 | 8 小时 | 前端组件 |
| **Day 4** | 测试验证 | 赵工 | 8 小时 | 测试报告 |
| **Day 5** | 灰度发布 | 孙工 | 4 小时 | 发布报告 |

---

## 六、验收标准

### 6.1 功能验收

- [ ] 正常流程：所有 API 正常，报告完整生成
- [ ] 部分失败：单个 API 失败，其他模块正常展示
- [ ] 全部失败：显示友好提示，不报错 500
- [ ] 历史查询：历史报告与生成时完全一致

### 6.2 技术验收

- [ ] 代码 Review 通过（张工签字）
- [ ] 单元测试覆盖率 > 80%
- [ ] 压测：100 并发下成功率 > 95%
- [ ] 监控告警配置完成

### 6.3 用户体验验收

- [ ] 错误提示友好，无技术术语
- [ ] 页面不白屏，布局不破坏
- [ ] 支持失败模块重试

---

**编制人**: 张工  
**审核人**: 待填写  
**批准人**: 待填写  
**日期**: 2026-02-25
