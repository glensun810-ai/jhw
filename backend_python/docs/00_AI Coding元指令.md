元指令”（Meta-Instruction）**。
这段提示词的设计逻辑是：先锁死权限，再明确路径，最后强制自检。

📥 通用开头提示词 (建议作为 AI 的 System Prompt 或对话首句)
指令：架构对齐与开发准则遵循
1.在开始任何编码任务前，请你必须先深入阅读并解析项目根目录下的两个核心规范文件：
REFACTOR_GUIDE.md：这是本项目的“宪法”，定义了全局架构分层和代码迁移的底线。
DEVELOPMENT_SOP.md：这是本项目的“操作手册”，定义了新功能开发的标准化步骤（Config -> API -> Service -> Page）。
在接下来的任务中，你必须严格遵守以下执行契约：
架构优先：禁止绕过 API 层直接在页面中使用原生请求 API。
位置感知：所有新定义的后端接口必须存放在 /api 目录，并从 /utils/config.js 读取 URL。
职责分离：复杂的逻辑计算必须进入 /services 层。
非破坏性原则：重构或修改代码时，必须确保原有函数签名不变，严禁造成存量功能崩溃。
如果你已经完全理解了这两份文档及上述要求，请简要说明你将如何处理我接下来的任务，然后等待我的具体需求指令。

💡 进阶技巧：如何让 AI 记忆更牢固？
1.如果你使用的是 Cursor 或 Windsurf 等工具，建议采取以下辅助手段：
2.使用 @ 符号强制引用： 在提具体需求时（例如：我想做一个支付页面），开头加上：
"请参考 @REFACTOR_GUIDE.md 和 @DEVELOPMENT_SOP.md，为我实现..."
设置 .cursorrules (针对 Cursor 用户)： 你可以将上面的提示词直接粘贴到项目根目录的 .cursorrules 文件中。这样每次你和它对话，它都会默认加载这些约束，不需要你每次手动复制粘贴。
定期“架构巡检”： 每当 AI 写完一段代码，你可以追加一句：
"请根据 DEVELOPMENT_SOP.md 的『违规自查清单』，对你刚刚生成的代码进行自我审计，并指出是否有任何不合规之处。"
