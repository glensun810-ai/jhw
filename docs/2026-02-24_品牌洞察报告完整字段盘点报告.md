# 品牌洞察报告页 - 完整字段盘点报告

**生成时间**: 2026-02-24  
**文档版本**: v1.0  
**分析范围**: 前端展示 + 后端数据 + 数据流

---

## 📑 目录

1. [页面头部](#1-页面头部)
2. [品牌概览区](#2-品牌概览区)
3. [核心洞察摘要区](#3-核心洞察摘要区)
4. [多维度分析区](#4-多维度分析区)
5. [AI 平台认知对比区](#5-ai 平台认知对比区)
6. [竞争分析区](#6-竞争分析区)
7. [语义偏移分析区](#7-语义偏移分析区)
8. [优化建议区](#8-优化建议区)
9. [关键词云区](#9-关键词云区)
10. [信源纯净度分析区](#10-信源纯净度分析区)
11. [信源情报分析区](#11-信源情报分析区)
12. [详细测试结果区](#12-详细测试结果区)
13. [底部操作栏](#13-底部操作栏)
14. [数据流映射表](#14-数据流映射表)

---

## 1. 页面头部

### 1.1 页面标题

| 字段名 | 类型 | 必填 | 说明 | 数据来源 |
|--------|------|------|------|----------|
| `targetBrand` | String | ✅ | 目标品牌名称 | 用户输入/URL 参数 |
| `pageSubtitle` | String | ✅ | 副标题文案 | 前端生成 |

**WXML 绑定**:
```html
<text class="page-title">品牌洞察报告</text>
<text class="page-subtitle">"{{targetBrand}}"：AI 认知与市场格局分析</text>
```

---

## 2. 品牌概览区

### 2.1 品牌评分卡片

| 字段名 | 类型 | 必填 | 说明 | 数据来源 |
|--------|------|------|------|----------|
| `competitiveAnalysis.brandScores[targetBrand].overallScore` | Number | ✅ | 综合评分 (0-100) | 后端生成/前端计算 |
| `competitiveAnalysis.brandScores[targetBrand].overallGrade` | String | ✅ | 等级 (A+/A/B/C/D) | 后端生成/前端计算 |
| `competitiveAnalysis.brandScores[targetBrand].overallSummary` | String | ✅ | 评分总结文案 | 后端生成/前端计算 |

**WXML 绑定**:
```html
<text class="score-number">{{competitiveAnalysis.brandScores[targetBrand].overallScore || 0}}</text>
<text class="score-grade">{{competitiveAnalysis.brandScores[targetBrand].overallGrade || 'D'}}</text>
<text class="score-desc">{{competitiveAnalysis.brandScores[targetBrand].overallSummary || '暂无评价'}}</text>
```

**后端计算逻辑** (`nxm_execution_engine.py`):
```python
brand_scores[brand] = {
    'overallScore': round(avg_score),  # 0-100
    'overallGrade': grade,  # A+/A/B/C/D
    'overallAuthority': round(avg_authority),
    'overallVisibility': round(avg_visibility),
    'overallPurity': round(avg_purity),
    'overallConsistency': round(avg_consistency),
    'overallSummary': f'GEO 综合评分为 {round(avg_score)} 分，等级为 {grade}'
}
```

---

## 3. 核心洞察摘要区

### 3.1 优势领域

| 字段名 | 类型 | 必填 | 说明 | 数据来源 |
|--------|------|------|------|----------|
| `advantageInsight` | String | ✅ | 优势领域描述 | 前端生成/后端提供 |

**WXML 绑定**:
```html
<view class="insight-card advantage-card">
  <view class="card-icon">🏆</view>
  <view class="card-title">优势领域</view>
  <view class="card-content">{{advantageInsight}}</view>
</view>
```

### 3.2 风险提示

| 字段名 | 类型 | 必填 | 说明 | 数据来源 |
|--------|------|------|------|----------|
| `riskInsight` | String | ✅ | 风险提示描述 | 前端生成/后端提供 |

### 3.3 机会点

| 字段名 | 类型 | 必填 | 说明 | 数据来源 |
|--------|------|------|------|----------|
| `opportunityInsight` | String | ✅ | 机会点描述 | 前端生成/后端提供 |

**默认值** (`results.js`):
```javascript
advantageInsight: '权威度表现突出，可见度良好',
riskInsight: '品牌纯净度有待提升',
opportunityInsight: '一致性方面有较大提升空间'
```

---

## 4. 多维度分析区

### 4.1 权威度 (Authority)

| 字段名 | 类型 | 必填 | 说明 | 数据来源 |
|--------|------|------|------|----------|
| `competitiveAnalysis.brandScores[targetBrand].overallAuthority` | Number | ✅ | 权威度分数 (0-100) | 后端生成/前端计算 |

### 4.2 可见度 (Visibility)

| 字段名 | 类型 | 必填 | 说明 | 数据来源 |
|--------|------|------|------|----------|
| `competitiveAnalysis.brandScores[targetBrand].overallVisibility` | Number | ✅ | 可见度分数 (0-100) | 后端生成/前端计算 |

### 4.3 纯净度 (Purity)

| 字段名 | 类型 | 必填 | 说明 | 数据来源 |
|--------|------|------|------|----------|
| `competitiveAnalysis.brandScores[targetBrand].overallPurity` | Number | ✅ | 纯净度分数 (0-100) | 后端生成/前端计算 |

### 4.4 一致性 (Consistency)

| 字段名 | 类型 | 必填 | 说明 | 数据来源 |
|--------|------|------|------|----------|
| `competitiveAnalysis.brandScores[targetBrand].overallConsistency` | Number | ✅ | 一致性分数 (0-100) | 后端生成/前端计算 |

**WXML 绑定**:
```html
<view class="dimension-card">
  <view class="dimension-header">
    <text class="dimension-name">权威度</text>
    <text class="dimension-score">{{competitiveAnalysis.brandScores[targetBrand].overallAuthority || 0}}分</text>
  </view>
  <view class="dimension-progress">
    <view class="progress-bar">
      <view class="progress-fill" style="width: {{competitiveAnalysis.brandScores[targetBrand].overallAuthority || 0}}%;"></view>
    </view>
  </view>
  <view class="dimension-status">{{getDimensionStatus(competitiveAnalysis.brandScores[targetBrand].overallAuthority || 0, 'authority')}}</view>
</view>
```

**状态判断函数** (`results.js`):
```javascript
getDimensionStatus: function(score, dimension) {
  if (score >= 80) return '优秀';
  if (score >= 60) return '良好';
  if (score >= 40) return '一般';
  return '待提升';
}
```

---

## 5. AI 平台认知对比区

### 5.1 平台标签页

| 字段名 | 类型 | 必填 | 说明 | 数据来源 |
|--------|------|------|------|----------|
| `platforms` | Array | ✅ | 平台列表 | 前端生成 |
| `platformDisplayNames` | Object | ✅ | 平台显示名称映射 | 前端生成 |
| `currentPlatform` | String | ✅ | 当前选中平台 | 前端状态 |

### 5.2 雷达图数据

| 字段名 | 类型 | 必填 | 说明 | 数据来源 |
|--------|------|------|------|----------|
| `radarChartData` | Array | ✅ | 雷达图数据数组 | 前端计算 |
| `radarChartRendered` | Boolean | ✅ | 是否已渲染 | 前端状态 |
| `canvasWidth` | Number | ✅ | Canvas 宽度 | 前端配置 |
| `canvasHeight` | Number | ✅ | Canvas 高度 | 前端配置 |

### 5.3 平台详情

| 字段名 | 类型 | 必填 | 说明 | 数据来源 |
|--------|------|------|------|----------|
| `pkDataByPlatform` | Object | ✅ | 各平台 PK 数据 | 前端计算 |

**WXML 绑定**:
```html
<view class="platform-tabs">
  <scroll-view scroll-x="true" class="tabs-scroll">
    <view class="tab-item {{currentPlatform === item ? 'active' : ''}}"
          wx:for="{{platforms}}" wx:key="platform"
          bindtap="switchPlatform"
          data-platform="{{item}}">
      {{platformDisplayNames[item]}}
    </view>
  </scroll-view>
</view>

<view class="radar-chart-container">
  <view class="radar-canvas-wrapper" hidden="{{!(radarChartData && radarChartData.length > 0)}}">
    <canvas type="2d" id="radarChartCanvas" class="radar-chart-canvas"
            style="width: {{canvasWidth}}px; height: {{canvasHeight}}px;"></canvas>
  </view>
</view>

<view class="platform-details">
  <view class="detail-item" wx:for="{{pkDataByPlatform[currentPlatform]}}" wx:key="index">
    <text class="detail-brand">{{item.myBrandData.brand}}</text>
    <text class="detail-score">{{item.myBrandData.overallScore}}</text>
  </view>
</view>
```

---

## 6. 竞争分析区

### 6.1 品牌曝光排名

| 字段名 | 类型 | 必填 | 说明 | 数据来源 |
|--------|------|------|------|----------|
| `brandRankingList` | Array | ✅ | 品牌排名列表 | 前端计算 |

**WXML 绑定**:
```html
<view class="ranking-list">
  <view class="ranking-item" wx:for="{{brandRankingList}}" wx:key="index" wx:for-item="brand">
    <text class="ranking-position">{{index + 1}}</text>
    <text class="ranking-brand {{brand === targetBrand ? 'main-brand' : ''}}">{{brand}}</text>
  </view>
</view>
```

### 6.2 AI 平台首次提及率

| 字段名 | 类型 | 必填 | 说明 | 数据来源 |
|--------|------|------|------|----------|
| `firstMentionByPlatform` | Array | ✅ | 各平台首次提及率 | 前端计算 |
| `firstMentionByPlatform[].platform` | String | ✅ | 平台名称 | - |
| `firstMentionByPlatform[].rate` | Number | ✅ | 提及率 (%) | - |

### 6.3 流量拦截风险

| 字段名 | 类型 | 必填 | 说明 | 数据来源 |
|--------|------|------|------|----------|
| `interceptionRisks` | Array | ✅ | 拦截风险列表 | 前端计算 |
| `interceptionRisks[].type` | String | ✅ | 风险类型 | - |
| `interceptionRisks[].level` | String | ✅ | 风险等级 (low/medium/high) | - |
| `interceptionRisks[].description` | String | ✅ | 风险描述 | - |

### 6.4 竞品对比详情

| 字段名 | 类型 | 必填 | 说明 | 数据来源 |
|--------|------|------|------|----------|
| `competitorComparisonData` | Array | ✅ | 竞品对比数据 | 前端计算 |
| `competitorComparisonData[].competitor` | String | ✅ | 竞品名称 | - |
| `competitorComparisonData[].differentiationScore` | Number | ✅ | 差异化评分 | - |
| `competitorComparisonData[].commonKeywords` | Array | ✅ | 共同关键词 | - |
| `competitorComparisonData[].uniqueToBrand` | Array | ✅ | 我方优势关键词 | - |
| `competitorComparisonData[].uniqueToCompetitor` | Array | ✅ | 对方优势关键词 | - |
| `competitorComparisonData[].differentiationGap` | String | ✅ | 差异化建议 | - |

---

## 7. 语义偏移分析区

### 7.1 语义偏移概览

| 字段名 | 类型 | 必填 | 说明 | 数据来源 |
|--------|------|------|------|----------|
| `semanticDriftData` | Object | ✅ | 语义偏移数据 | 后端生成 |
| `semanticDriftData.driftScore` | Number | ✅ | 偏移分数 (0-100) | 后端生成 |
| `semanticDriftData.driftSeverity` | String | ✅ | 偏移严重程度 (low/medium/high) | 后端生成 |
| `semanticDriftData.driftSeverityText` | String | ✅ | 偏移程度文本 | 后端生成 |
| `semanticDriftData.similarityScore` | Number | ✅ | 语义相似度 (%) | 后端生成 |

**WXML 绑定**:
```html
<view class="drift-score-card">
  <view class="score-circle {{semanticDriftData.driftSeverity}}">
    <text class="score-number">{{semanticDriftData.driftScore}}</text>
    <text class="score-label">偏移分数</text>
  </view>
  <view class="drift-info">
    <text class="drift-severity {{semanticDriftData.driftSeverity}}">{{semanticDriftData.driftSeverityText}}</text>
    <text class="drift-similarity">语义相似度：{{semanticDriftData.similarityScore}}%</text>
  </view>
</view>
```

### 7.2 关键词对比

| 字段名 | 类型 | 必填 | 说明 | 数据来源 |
|--------|------|------|------|----------|
| `semanticContrastData` | Object | ✅ | 语义对比数据 | 后端生成 |
| `semanticContrastData.official_words` | Array | ✅ | 官方关键词列表 | 后端生成 |
| `semanticContrastData.official_words[].name` | String | ✅ | 关键词名称 | - |
| `semanticContrastData.official_words[].value` | Number | ✅ | 权重值 | - |
| `semanticContrastData.official_words[].sentiment_valence` | Number | ✅ | 情感价 (0-1) | - |
| `semanticContrastData.official_words[].category` | String | ✅ | 类别 | - |
| `semanticContrastData.ai_generated_words` | Array | ✅ | AI 生成关键词列表 | 后端生成 |
| `semanticContrastData.ai_generated_words[].name` | String | ✅ | 关键词名称 | - |
| `semanticContrastData.ai_generated_words[].value` | Number | ✅ | 权重值 | - |
| `semanticContrastData.ai_generated_words[].sentiment_valence` | Number | ✅ | 情感价 (0-1) | - |
| `semanticContrastData.ai_generated_words[].category` | String | ✅ | 类别 | - |

### 7.3 偏移详情

| 字段名 | 类型 | 必填 | 说明 | 数据来源 |
|--------|------|------|------|----------|
| `semanticDriftData.missingKeywords` | Array | ✅ | 缺失关键词（官方有但 AI 未提及） | 后端生成 |
| `semanticDriftData.unexpectedKeywords` | Array | ✅ | 意外关键词（AI 提及但官方没有） | 后端生成 |
| `semanticDriftData.negativeTerms` | Array | ✅ | 负面术语 | 后端生成 |
| `semanticDriftData.positiveTerms` | Array | ✅ | 正面术语 | 后端生成 |

---

## 8. 优化建议区

### 8.1 建议概览

| 字段名 | 类型 | 必填 | 说明 | 数据来源 |
|--------|------|------|------|----------|
| `recommendationData` | Object | ✅ | 优化建议数据 | 后端生成 |
| `recommendationData.totalCount` | Number | ✅ | 建议总数 | 后端生成 |
| `recommendationData.highPriorityCount` | Number | ✅ | 高优先级数量 | 后端生成 |
| `recommendationData.mediumPriorityCount` | Number | ✅ | 中优先级数量 | 后端生成 |
| `recommendationData.lowPriorityCount` | Number | ✅ | 低优先级数量 | 后端生成 |

### 8.2 建议列表

| 字段名 | 类型 | 必填 | 说明 | 数据来源 |
|--------|------|------|------|----------|
| `recommendationData.recommendations` | Array | ✅ | 建议列表 | 后端生成 |
| `recommendationData.recommendations[].title` | String | ✅ | 建议标题 | - |
| `recommendationData.recommendations[].description` | String | ✅ | 建议描述 | - |
| `recommendationData.recommendations[].category` | String | ✅ | 类别 (content/pr/technical) | - |
| `recommendationData.recommendations[].phase` | String | ✅ | 阶段 (short_term/mid_term/long_term) | - |
| `recommendationData.recommendations[].priority` | String | ✅ | 优先级 (low/medium/high) | - |
| `recommendationData.recommendations[].priorityScore` | Number | ✅ | 优先级分数 | - |
| `recommendationData.recommendations[].urgency` | Number | ✅ | 紧急度 (1-10) | - |
| `recommendationData.recommendations[].estimatedImpact` | String | ✅ | 预估影响 (low/medium/high) | - |
| `recommendationData.recommendations[].target` | String | ✅ | 目标对象 | - |
| `recommendationData.recommendations[].actionSteps` | Array | ✅ | 行动步骤 | - |

**WXML 绑定**:
```html
<view class="recommendation-card" wx:for="{{recommendationData.recommendations}}" wx:key="index">
  <view class="card-header {{item.priority}}">
    <text class="priority-tag {{item.priority}}">{{item.priorityText}}</text>
    <text class="type-tag {{item.type}}">{{item.typeText}}</text>
    <text class="urgency-score">紧急度：{{item.urgency}}/10</text>
  </view>
  <view class="card-body">
    <text class="card-title">{{item.title}}</text>
    <text class="card-description">{{item.description}}</text>
    <view class="action-steps" wx:if="{{item.actionSteps && item.actionSteps.length > 0}}">
      <text class="steps-title">📋 行动步骤：</text>
      <view class="step-item" wx:for="{{item.actionSteps}}" wx:key="index">
        <text class="step-number">{{index + 1}}</text>
        <text class="step-text">{{item}}</text>
      </view>
    </view>
  </view>
</view>
```

---

## 9. 关键词云区

### 9.1 词云数据

| 字段名 | 类型 | 必填 | 说明 | 数据来源 |
|--------|------|------|------|----------|
| `keywordCloudData` | Array | ✅ | 词云数据数组 | 前端计算 |
| `topKeywords` | Array | ✅ | 高频词列表 | 前端计算 |
| `keywordStats` | Object | ✅ | 关键词统计 | 前端计算 |
| `keywordStats.positiveCount` | Number | ✅ | 正面词数量 | - |
| `keywordStats.neutralCount` | Number | ✅ | 中性词数量 | - |
| `keywordStats.negativeCount` | Number | ✅ | 负面词数量 | - |
| `wordCloudCanvasWidth` | Number | ✅ | Canvas 宽度 | 前端配置 |
| `wordCloudCanvasHeight` | Number | ✅ | Canvas 高度 | 前端配置 |
| `wordCloudRendered` | Boolean | ✅ | 是否已渲染 | 前端状态 |

### 9.2 高频词列表

| 字段名 | 类型 | 必填 | 说明 | 数据来源 |
|--------|------|------|------|----------|
| `topKeywords[].word` | String | ✅ | 词语 | - |
| `topKeywords[].count` | Number | ✅ | 出现次数 | - |
| `topKeywords[].sentiment` | String | ✅ | 情感 (positive/neutral/negative) | - |

---

## 10. 信源纯净度分析区

### 10.1 信源纯净度概览

| 字段名 | 类型 | 必填 | 说明 | 数据来源 |
|--------|------|------|------|----------|
| `sourcePurityData` | Object | ✅ | 信源纯净度数据 | 后端生成 |
| `sourcePurityData.purityScore` | Number | ✅ | 纯净度分数 (0-100) | 后端生成 |
| `sourcePurityData.purityLevel` | String | ✅ | 纯净度等级 (low/medium/high) | 后端生成 |
| `sourcePurityData.purityLevelText` | String | ✅ | 纯净度文本 | 后端生成 |
| `sourcePurityData.highWeightRatio` | Number | ✅ | 高权重信源占比 (%) | 后端生成 |
| `sourcePurityData.pollutionCount` | Number | ✅ | 污染源数量 | 后端生成 |

### 10.2 信源类别分布

| 字段名 | 类型 | 必填 | 说明 | 数据来源 |
|--------|------|------|------|----------|
| `sourcePurityData.categoryDistribution` | Array | ✅ | 类别分布 | 后端生成 |
| `sourcePurityData.categoryDistribution[].category` | String | ✅ | 类别代码 | - |
| `sourcePurityData.categoryDistribution[].categoryName` | String | ✅ | 类别名称 | - |
| `sourcePurityData.categoryDistribution[].count` | Number | ✅ | 数量 | - |
| `sourcePurityData.categoryDistribution[].percentage` | Number | ✅ | 占比 (%) | - |

### 10.3 信源权重排名

| 字段名 | 类型 | 必填 | 说明 | 数据来源 |
|--------|------|------|------|----------|
| `sourcePurityData.topSources` | Array | ✅ | 高权重信源列表 | 后端生成 |
| `sourcePurityData.topSources[].name` | String | ✅ | 信源名称 | - |
| `sourcePurityData.topSources[].weight` | Number | ✅ | 权重值 | - |
| `sourcePurityData.topSources[].isHighWeight` | Boolean | ✅ | 是否高权重 | - |

### 10.4 污染源警示

| 字段名 | 类型 | 必填 | 说明 | 数据来源 |
|--------|------|------|------|----------|
| `sourcePurityData.pollutionSources` | Array | ✅ | 污染源列表 | 后端生成 |
| `sourcePurityData.pollutionSources[].name` | String | ✅ | 污染源名称 | - |
| `sourcePurityData.pollutionSources[].reason` | String | ✅ | 污染原因 | - |

---

## 11. 信源情报分析区

### 11.1 信源情报图谱

| 字段名 | 类型 | 必填 | 说明 | 数据来源 |
|--------|------|------|------|----------|
| `sourceIntelligenceMap` | Object | ✅ | 信源情报图谱 | 后端生成 |
| `sourceIntelligenceMap.nodes` | Array | ✅ | 图谱节点 | 后端生成 |
| `sourceIntelligenceMap.nodes[].id` | String | ✅ | 节点 ID | - |
| `sourceIntelligenceMap.nodes[].name` | String | ✅ | 节点名称 | - |
| `sourceIntelligenceMap.nodes[].value` | Number | ✅ | 权重值 | - |
| `sourceIntelligenceMap.nodes[].sentiment` | String | ✅ | 情感倾向 | - |
| `sourceIntelligenceMap.nodes[].category` | String | ✅ | 类别 | - |
| `showSourceIntelligence` | Boolean | ✅ | 是否显示 | 前端状态 |

---

## 12. 详细测试结果区

### 12.1 视图控制

| 字段名 | 类型 | 必填 | 说明 | 数据来源 |
|--------|------|------|------|----------|
| `currentViewMode` | String | ✅ | 当前视图模式 (brand/dimension/detailed) | 前端状态 |
| `groupedResultsByBrand` | Array | ✅ | 按品牌分组的结果 | 前端计算 |
| `dimensionComparisonData` | Array | ✅ | 维度对比数据 | 前端计算 |
| `expandedBrands` | Object | ✅ | 展开的品牌详情 | 前端状态 |

### 12.2 品牌对比视图

| 字段名 | 类型 | 必填 | 说明 | 数据来源 |
|--------|------|------|------|----------|
| `groupedResultsByBrand[].brand` | String | ✅ | 品牌名称 | - |
| `groupedResultsByBrand[].isMainBrand` | Boolean | ✅ | 是否主品牌 | - |
| `groupedResultsByBrand[].overallScore` | Number | ✅ | 综合得分 | - |
| `groupedResultsByBrand[].scores` | Object | ✅ | 各维度得分 | - |
| `groupedResultsByBrand[].scores.authority_score` | Number | ✅ | 权威度得分 | - |
| `groupedResultsByBrand[].scores.visibility_score` | Number | ✅ | 可见度得分 | - |
| `groupedResultsByBrand[].scores.purity_score` | Number | ✅ | 纯净度得分 | - |
| `groupedResultsByBrand[].scores.consistency_score` | Number | ✅ | 一致性得分 | - |
| `groupedResultsByBrand[].questions` | Array | ✅ | 问题列表 | - |

### 12.3 维度对比视图

| 字段名 | 类型 | 必填 | 说明 | 数据来源 |
|--------|------|------|------|----------|
| `dimensionComparisonData[].dimension` | String | ✅ | 维度名称 | - |
| `dimensionComparisonData[].icon` | String | ✅ | 维度图标 | - |
| `dimensionComparisonData[].averageScore` | Number | ✅ | 平均分 | - |
| `dimensionComparisonData[].brands` | Array | ✅ | 品牌数据 | - |
| `dimensionComparisonData[].brands[].brand` | String | ✅ | 品牌名称 | - |
| `dimensionComparisonData[].brands[].isMainBrand` | Boolean | ✅ | 是否主品牌 | - |
| `dimensionComparisonData[].brands[].score` | Number | ✅ | 得分 | - |

### 12.4 详细视图

| 字段名 | 类型 | 必填 | 说明 | 数据来源 |
|--------|------|------|------|----------|
| `latestTestResults` | Array | ✅ | 最新测试结果 | 后端返回 |
| `latestTestResults[].brand` | String | ✅ | 品牌名称 | - |
| `latestTestResults[].question` | String | ✅ | 问题 | - |
| `latestTestResults[].aiModel` | String | ✅ | AI 模型 | - |
| `latestTestResults[].response` | String | ✅ | 响应内容 | - |
| `latestTestResults[].authority_score` | Number | ✅ | 权威度得分 | - |
| `latestTestResults[].visibility_score` | Number | ✅ | 可见度得分 | - |
| `latestTestResults[].purity_score` | Number | ✅ | 纯净度得分 | - |
| `latestTestResults[].consistency_score` | Number | ✅ | 一致性得分 | - |
| `latestTestResults[].score` | Number | ✅ | 总分 | - |

---

## 13. 底部操作栏

### 13.1 操作按钮

| 字段名 | 类型 | 必填 | 说明 | 数据来源 |
|--------|------|------|------|----------|
| `showSaveResultModal` | Boolean | ✅ | 是否显示保存弹窗 | 前端状态 |
| `saveBrandName` | String | ✅ | 保存的品牌名称 | 前端状态 |
| `saveTags` | Array | ✅ | 标签列表 | 前端状态 |
| `saveCategories` | Array | ✅ | 分类列表 | 前端配置 |
| `saveCategoryIndex` | Number | ✅ | 选中分类索引 | 前端状态 |
| `selectedSaveCategory` | String | ✅ | 选中分类 | 前端状态 |
| `saveNotes` | String | ✅ | 备注 | 前端状态 |
| `saveAsFavorite` | Boolean | ✅ | 是否收藏 | 前端状态 |

---

## 14. 数据流映射表

### 14.1 后端 → 前端数据映射

| 后端字段 | 前端字段 | 转换逻辑 | 状态 |
|---------|---------|---------|------|
| `brand_scores` | `competitiveAnalysis.brandScores` | 直接使用 | ✅ 已修复 |
| `semantic_drift_data` | `semanticDriftData` | 直接使用 | ✅ 已修复 |
| `recommendation_data` | `recommendationData` | 直接使用 | ✅ 已修复 |
| `negative_sources` | `sourcePurityData` | 转换格式 | ⚠️ 需核实 |
| `competitive_analysis` | `competitiveAnalysis` | 直接使用 | ✅ 已修复 |
| `detailed_results` | `latestTestResults` | 直接使用 | ✅ 已修复 |

### 14.2 前端计算字段

| 字段名 | 计算逻辑 | 依赖数据 |
|--------|---------|---------|
| `brandRankingList` | 按品牌评分排序 | `brandScores` |
| `firstMentionByPlatform` | 统计首次提及率 | `latestTestResults` |
| `interceptionRisks` | 分析拦截风险 | `latestTestResults` |
| `competitorComparisonData` | 竞品对比分析 | `latestTestResults` |
| `groupedResultsByBrand` | 按品牌分组 | `latestTestResults` |
| `dimensionComparisonData` | 维度对比分析 | `latestTestResults` |
| `pkDataByPlatform` | 平台 PK 数据 | `latestTestResults` |
| `radarChartData` | 雷达图数据 | `brandScores` |
| `keywordCloudData` | 词云数据 | `latestTestResults` |

### 14.3 数据来源状态

| 数据模块 | 后端生成 | 前端计算 | 状态 |
|---------|---------|---------|------|
| 品牌评分 | ✅ | ✅ (备用) | ✅ 正常 |
| 核心洞察 | ⚠️ | ✅ (默认) | ⚠️ 需完善 |
| 多维度分析 | ✅ | ✅ (备用) | ✅ 正常 |
| AI 平台对比 | ❌ | ✅ | ✅ 正常 |
| 竞争分析 | ⚠️ | ✅ | ⚠️ 需竞品数据 |
| 语义偏移 | ✅ | ❌ | ✅ 正常 |
| 优化建议 | ✅ | ❌ | ✅ 正常 |
| 关键词云 | ❌ | ✅ | ✅ 正常 |
| 信源纯净度 | ✅ | ❌ | ⚠️ 需核实 |
| 详细结果 | ✅ | ❌ | ✅ 正常 |

---

## 📋 修复状态总结

### ✅ 已修复
1. 品牌评分计算（后端 + 前端备用）
2. 多维度分析分数计算
3. 前端数据验证逻辑
4. 高级分析数据解析

### ⚠️ 待完善
1. 核心洞察文案生成（依赖后端）
2. 竞品数据生成（需要 NxM 遍历所有品牌）
3. 信源纯净度数据生成（需要检查后端逻辑）

### ❌ 缺失
1. 信源情报图谱数据（后端未生成）

---

**文档结束**

**生成时间**: 2026-02-24  
**文档版本**: v1.0  
**维护人**: 首席测试工程师 & 首席全栈开发工程师
