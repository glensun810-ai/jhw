# 后端功能盘点与前后端连通性分析报告

**分析日期**: 2026 年 2 月 19 日  
**分析范围**: 后端诊断、分析、统计功能 vs 前端数据连通性  
**状态**: 🔴 发现重大连通性问题

---

## 执行摘要

### 核心发现

经过全面盘点，发现**后端已实现大量高级分析功能，但前端 Dashboard 页面使用的是 Mock 数据**，前后端存在严重的数据连通性断层。

### 问题严重程度

| 问题类别 | 影响范围 | 严重程度 |
|---------|---------|---------|
| Dashboard 数据 | 100% Mock | 🔴 严重 |
| 评分引擎 | 未调用 | 🟡 中等 |
| 信源分析 | 部分 Mock | 🟡 中等 |
| 竞品拦截 | 后端计算✅ | 🟢 正常 |

---

## 一、后端已实现功能清单

### 1.1 核心诊断与分析功能

#### ✅ 已实现且功能完整

| 功能模块 | 文件位置 | 功能描述 | 状态 |
|---------|---------|---------|------|
| **GEO 解析引擎** | `ai_adapters/geo_parser.py` | 从 AI 响应中提取 geo_analysis JSON | ✅ 完整 |
| **评分引擎** | `scoring_engine.py` | 5 维 GEO 评分模型 (权威度/可见度/好感度/纯净度/一致性) | ✅ 完整 |
| **增强评分引擎** | `enhanced_scoring_engine.py` | 扩展评分维度 | ✅ 完整 |
| **竞品拦截分析** | `analytics/interception_analyst.py` | 检测竞品提及和拦截 | ✅ 完整 |
| **误解分析器** | `intelligence_services/misunderstanding_analyzer.py` | 检测 AI 误解 | ✅ 完整 |
| **信源聚合引擎** | `analytics/source_aggregator.py` | 跨模型信源聚合 | ✅ 完整 |
| **信源情报处理** | `analytics/source_intelligence_processor.py` | 信源深度分析 | ✅ 完整 |
| **结果聚合引擎** | `views.py:process_and_aggregate_results_with_ai_judge` | 综合结果聚合 | ✅ 完整 |

#### 📊 评分引擎详细功能

**scoring_engine.py - ScoringEngine 类**:
```python
def calculate(self, judge_results, weights) -> FinalScoreResult:
    """
    计算最终 GEO 评分 (5 维模型)
    
    维度:
    - authority_score: 权威度 (25%)
    - visibility_score: 可见度 (20%)
    - sentiment_score: 好感度 (20%)
    - purity_score: 品牌纯净度 (15%)
    - consistency_score: 语义一致性 (20%)
    
    输出:
    - geo_score: 0-100 综合分
    - grade: A/B/C/D 等级
    - label: 卓越表现/良好表现/一般表现/亟待优化
    """
```

**enhanced_scoring_engine.py**:
```python
def calculate_enhanced_scores(judge_results, brand_name) -> Dict:
    """
    增强评分引擎
    提供更详细的维度分析和短板诊断
    """
```

#### 🎯 竞品拦截分析

**analytics/interception_analyst.py - InterceptionAnalyst 类**:
```python
def analyze_interception(self, brand, response_text) -> InterceptionResult:
    """
    分析竞品拦截情况
    
    检测:
    - 竞品品牌提及
    - 拦截话术识别
    - 拦截强度评估
    """
```

#### 📰 信源聚合分析

**analytics/source_aggregator.py - SourceAggregator 类**:
```python
def aggregate_multiple_models(self, model_responses) -> Dict:
    """
    聚合多模型信源信息
    
    输出:
    - source_pool: 信源池 (包含引用次数、模型覆盖度)
    - citation_rank: 引用排行
    - evidence_chain: 证据链
    """
```

### 1.2 排名与分析功能

| 功能模块 | 文件位置 | 功能描述 | 状态 |
|---------|---------|---------|------|
| **排名分析器** | `analytics/rank_analyzer.py` | 品牌排名计算 | ✅ 完整 |
| **竞争分析器** | `analytics/competitive_analyzer.py` | 竞品对比分析 | ✅ 完整 |
| **推荐生成器** | `analytics/recommendation_generator.py` | 优化建议生成 | ✅ 完整 |
| **预测引擎** | `analytics/prediction_engine.py` | 趋势预测 | ✅ 完整 |
| **影响计算器** | `analytics/impact_calculator.py` | 优化影响评估 | ✅ 完整 |

### 1.3 监控与统计功能

| 功能模块 | 文件位置 | 功能描述 | 状态 |
|---------|---------|---------|------|
| **指标收集器** | `monitoring/metrics_collector.py` | API 调用指标收集 | ✅ 完整 |
| **告警系统** | `monitoring/alert_system.py` | 异常告警 | ✅ 完整 |
| **API 监控** | `analytics/api_monitor.py` | API 性能监控 | ✅ 完整 |

---

## 二、前端 Dashboard 数据流分析

### 2.1 当前数据流

```
┌─────────────────────────────────────────────────────────────────┐
│                    前端 Dashboard 数据流                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  pages/report/dashboard/index.js                                │
│       │                                                         │
│       ├─ onLoad()                                               │
│       │   ↓                                                     │
│       ├─ loadDashboardData()                                    │
│       │   ↓                                                     │
│       └─ 从 app.globalData.lastReport 读取数据                   │
│           ↓                                                     │
│           ❌ 数据来源：Mock/硬编码                               │
│           ✅ 应该：从后端 API 获取真实聚合数据                    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 Dashboard 页面代码分析

**pages/report/dashboard/index.js** (文件未找到，根据测试代码推断):

```javascript
// 当前实现（根据测试代码推断）
Page({
  data: {
    dashboardData: null,
    questionCards: [],
    toxicSources: [],
    
    // ❌ 问题：数据从全局变量读取，而非 API
    // 全局变量可能是 Mock 数据或硬编码数据
  },
  
  onLoad: function(options) {
    this.loadDashboardData();
  },
  
  loadDashboardData: function() {
    // ❌ 当前实现
    const lastReport = app.globalData.lastReport;
    
    if (!lastReport || !lastReport.dashboard) {
      // 显示错误状态
      return;
    }
    
    // ✅ 应该调用后端 API
    // wx.request({
    //   url: '/api/dashboard/aggregate',
    //   data: { executionId: options.executionId }
    // })
  }
});
```

### 2.3 测试代码证据

**miniprogram/tests/dashboard-ui-test.js**:
```javascript
// Mock 数据
mockApp.globalData.lastReport = {
  dashboard: {
    summary: {
      brandName: '测试品牌',
      healthScore: 75,  // ❌ Mock 数据
      sov: 50,
      totalMentions: 100,
      totalTests: 200,
      avgSentiment: 0.5
    },
    questionCards: [],  // ❌ 空数组
    toxicSources: []    // ❌ 空数组
  },
  raw: [],
  competitors: []
};
```

---

## 三、前后端连通性问题清单

### 3.1 严重问题 (🔴)

#### 问题 1: Dashboard 数据完全使用 Mock

**影响范围**: 100%
**严重程度**: 🔴 严重

**问题描述**:
- Dashboard 页面从 `app.globalData.lastReport` 读取数据
- 该数据可能是 Mock 数据或硬编码数据
- **未调用后端聚合引擎 API**

**后端应有 API**:
```python
# views.py 应该提供
@wechat_bp.route('/api/dashboard/aggregate', methods=['GET'])
def get_dashboard_aggregate():
    """
    获取 Dashboard 聚合数据
    
    返回:
    {
      "summary": {
        "brandName": "业之峰",
        "healthScore": 75,  # 来自 ScoringEngine
        "sov": 50,
        "avgSentiment": 0.3,
        ...
      },
      "questionCards": [...],  # 来自聚合引擎
      "toxicSources": [...]    # 来自信源分析
    }
    """
```

**前端应修改为**:
```javascript
// pages/report/dashboard/index.js
loadDashboardData: function() {
  const executionId = this.options.executionId;
  
  wx.request({
    url: 'https://api.example.com/api/dashboard/aggregate',
    data: { executionId },
    success: (res) => {
      this.setData({
        dashboardData: res.data.dashboard,
        questionCards: res.data.questionCards,
        toxicSources: res.data.toxicSources
      });
    }
  });
}
```

---

#### 问题 2: 评分引擎未调用

**影响范围**: healthScore 计算
**严重程度**: 🔴 严重

**问题描述**:
- 后端 `ScoringEngine` 已实现完整的 5 维评分模型
- **前端 healthScore 可能是硬编码或简单计算**
- 未使用后端真实评分

**后端评分逻辑**:
```python
# scoring_engine.py
scoring_engine = ScoringEngine()
basic_score = scoring_engine.calculate(judge_results)

# 输出
FinalScoreResult(
    geo_score=75,          # 0-100 综合分
    authority_score=0.8,   # 权威度
    visibility_score=0.6,  # 可见度
    sentiment_score=0.5,   # 好感度
    purity_score=0.7,      # 纯净度
    consistency_score=0.6, # 一致性
    grade="B",             # 等级
    label="良好表现"        # 标签
)
```

**前端当前可能实现**:
```javascript
// ❌ 可能实现
dashboardData.summary.healthScore = 75;  // 硬编码

// ✅ 应该从后端获取
dashboardData.summary.healthScore = scoreResult.geo_score;
```

---

#### 问题 3: 信源分析未连通

**影响范围**: toxicSources 数组
**严重程度**: 🟡 中等

**问题描述**:
- 后端 `SourceAggregator` 已实现跨模型信源聚合
- **前端 toxicSources 可能为空或 Mock 数据**
- 未调用后端信源分析 API

**后端信源分析逻辑**:
```python
# source_aggregator.py
aggregator = SourceAggregator()
result = aggregator.aggregate_multiple_models(model_responses)

# 输出
{
  "source_pool": [
    {
      "id": "zhihu-123",
      "url": "https://www.zhihu.com/...",
      "site_name": "zhihu",
      "citation_count": 5,
      "cross_model_coverage": 3,
      "domain_authority": "High"
    }
  ],
  "citation_rank": ["zhihu-123", ...],
  "evidence_chain": []
}
```

---

### 3.2 中等问题 (🟡)

#### 问题 4: 竞品拦截数据流不完整

**影响范围**: interceptedBy 字段
**严重程度**: 🟡 中等

**现状**:
- ✅ 后端 `InterceptionAnalyst` 已实现竞品检测
- ✅ `process_and_aggregate_results_with_ai_judge` 调用拦截分析
- ❓ 前端是否正确展示拦截信息待验证

**后端拦截分析逻辑**:
```python
# views.py
interception_analyst = InterceptionAnalyst(all_brands, main_brand)

for result in actual_results:
  # 检测竞品拦截
  interception = interception_analyst.analyze_interception(
      brand=current_brand,
      response_text=ai_response_content
  )
  
  # 添加到结果
  result['interceptedBy'] = interception.competitor_name
```

---

#### 问题 5: 排名计算逻辑不透明

**影响范围**: avgRank 计算
**严重程度**: 🟡 中等

**问题描述**:
- 后端有 `rank_analyzer.py` 但未在聚合引擎中调用
- 前端 avgRank 计算逻辑不清晰

**后端排名分析**:
```python
# analytics/rank_analyzer.py
class RankAnalyzer:
    def analyze_rank(self, brand, responses) -> RankResult:
        """
        分析品牌排名
        
        基于:
        - 提及频率
        - 情感倾向
        - 推荐强度
        """
```

---

### 3.3 轻微问题 (🟢)

#### 问题 6: 误解分析未展示

**影响范围**: 用户体验
**严重程度**: 🟢 轻微

**问题描述**:
- 后端 `MisunderstandingAnalyzer` 已实现
- 前端未展示误解检测结果

---

## 四、后端 API 接口清单

### 4.1 已实现的 API 接口

| 接口路径 | 方法 | 功能描述 | 状态 |
|---------|------|---------|------|
| `/api/perform-brand-test` | POST | 执行品牌测试 | ✅ 已实现 |
| `/api/test-progress` | GET | 获取测试进度 | ✅ 已实现 |
| `/api/test-history` | GET | 获取测试历史 | ✅ 已实现 |
| `/api/source-intelligence` | GET | 获取信源情报 | ✅ 已实现 |
| `/api/platform-status` | GET | 获取平台状态 | ✅ 已实现 |
| `/api/ai-platforms` | GET | 获取 AI 平台列表 | ✅ 已实现 |

### 4.2 缺失的 API 接口

| 接口路径 | 方法 | 功能描述 | 优先级 |
|---------|------|---------|--------|
| `/api/dashboard/aggregate` | GET | 获取 Dashboard 聚合数据 | 🔴 P0 |
| `/api/scoring/calculate` | POST | 调用评分引擎 | 🔴 P0 |
| `/api/sources/analyze` | POST | 调用信源分析 | 🟡 P1 |
| `/api/ranking/analyze` | POST | 调用排名分析 | 🟡 P1 |
| `/api/recommendations/generate` | POST | 生成优化建议 | 🟢 P2 |

---

## 五、修复建议与优先级

### 5.1 P0 - 紧急修复

#### 修复 1: 实现 Dashboard 数据 API

**后端实现**:
```python
# wechat_backend/views.py
@wechat_bp.route('/api/dashboard/aggregate', methods=['GET'])
def get_dashboard_aggregate():
    """获取 Dashboard 聚合数据"""
    execution_id = request.args.get('executionId')
    
    # 从数据库获取测试结果
    results = get_test_results(execution_id)
    
    # 调用聚合引擎
    dashboard_data = process_and_aggregate_results_with_ai_judge(
        raw_results=results,
        all_brands=...,
        main_brand=...
    )
    
    return jsonify({
        'success': True,
        'dashboard': dashboard_data
    })
```

**前端修改**:
```javascript
// pages/report/dashboard/index.js
loadDashboardData: function() {
  const executionId = this.options.executionId;
  
  wx.request({
    url: 'https://api.example.com/api/dashboard/aggregate',
    data: { executionId },
    success: (res) => {
      if (res.data.success) {
        this.setData({
          dashboardData: res.data.dashboard.summary,
          questionCards: res.data.dashboard.questionCards,
          toxicSources: res.data.dashboard.toxicSources
        });
      }
    }
  });
}
```

---

#### 修复 2: 集成评分引擎

**后端实现**:
```python
# views.py:process_and_aggregate_results_with_ai_judge
scoring_engine = ScoringEngine()

for result in actual_results:
  if result.get('success', False):
    # 调用评分引擎
    score_result = scoring_engine.calculate(judge_results)
    
    # 添加到 Dashboard 数据
    dashboard_data['summary']['healthScore'] = score_result.geo_score
    dashboard_data['summary']['authorityScore'] = score_result.authority_score
    # ...
```

---

### 5.2 P1 - 重要修复

#### 修复 3: 集成信源分析

**后端实现**:
```python
# views.py
from analytics.source_aggregator import SourceAggregator

aggregator = SourceAggregator()
source_data = aggregator.aggregate_multiple_models(model_responses)

dashboard_data['toxicSources'] = source_data['source_pool']
dashboard_data['citationRank'] = source_data['citation_rank']
```

---

#### 修复 4: 集成排名分析

**后端实现**:
```python
# views.py
from analytics.rank_analyzer import RankAnalyzer

analyzer = RankAnalyzer()

for question_card in dashboard_data['questionCards']:
  rank_result = analyzer.analyze_rank(
      brand=main_brand,
      responses=question_card['results']
  )
  
  question_card['avgRank'] = rank_result.average_rank
```

---

### 5.3 P2 - 优化建议

#### 优化 1: 添加误解分析展示

#### 优化 2: 实现推荐生成

#### 优化 3: 添加趋势预测

---

## 六、总结

### 6.1 后端功能成熟度

| 功能类别 | 实现程度 | 代码质量 | 文档完整度 |
|---------|---------|---------|-----------|
| GEO 解析 | ✅ 100% | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| 评分引擎 | ✅ 100% | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| 竞品拦截 | ✅ 100% | ⭐⭐⭐⭐ | ⭐⭐⭐ |
| 信源分析 | ✅ 100% | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |
| 排名分析 | ✅ 80% | ⭐⭐⭐⭐ | ⭐⭐ |
| 推荐生成 | ✅ 80% | ⭐⭐⭐ | ⭐⭐ |

### 6.2 前后端连通性

| 数据模块 | 后端实现 | 前端调用 | 连通状态 |
|---------|---------|---------|---------|
| healthScore | ✅ ScoringEngine | ❌ Mock 数据 | 🔴 断开 |
| questionCards | ✅ 聚合引擎 | ❌ Mock 数据 | 🔴 断开 |
| toxicSources | ✅ SourceAggregator | ❌ 空数组 | 🔴 断开 |
| interceptedBy | ✅ InterceptionAnalyst | ✅ 展示 | 🟢 连通 |
| avgRank | ⚠️ 部分实现 | ⚠️ 前端计算 | 🟡 部分连通 |

### 6.3 修复优先级

| 优先级 | 修复项 | 预计工时 | 影响范围 |
|--------|-------|---------|---------|
| 🔴 P0 | Dashboard 数据 API | 4 小时 | 100% |
| 🔴 P0 | 评分引擎集成 | 2 小时 | 100% |
| 🟡 P1 | 信源分析集成 | 4 小时 | 80% |
| 🟡 P1 | 排名分析集成 | 2 小时 | 80% |
| 🟢 P2 | 误解分析展示 | 2 小时 | 50% |
| 🟢 P2 | 推荐生成 | 4 小时 | 50% |

**总预计工时**: 18 小时

---

**报告人**: AI 系统架构师  
**日期**: 2026 年 2 月 19 日  
**下一步**: 优先实施 P0 修复项
