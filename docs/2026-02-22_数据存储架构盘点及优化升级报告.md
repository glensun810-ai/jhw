# 数据存储架构盘点及优化升级报告

**报告编号**: STORAGE-AUDIT-2026-0222-001  
**分析日期**: 2026-02-22  
**分析工程师**: AI Assistant (系统数据专家)  
**分析级别**: 🔴 P0 - 全面审计

---

## 📋 执行摘要

### 存储资产总览

| 存储类型 | 数量 | 总大小 | 状态 |
|----------|------|--------|------|
| SQLite 数据库 | 3 个 | ~580KB | ⚠️ 部分闲置 |
| JSONL 日志文件 | 1 个 | 572KB | ✅ 活跃 |
| 小程序本地存储 | N/A | 未知 | ⚠️ 需验证 |

### 关键发现

| 发现 | 严重性 | 状态 |
|------|--------|------|
| `ai_responses.jsonl` 正常记录数据 (139 条) | ℹ️ 信息 | ✅ 正常 |
| `brand_test.db` 表全空 (0 条记录) | 🔴 严重 | ❌ 功能未完成 |
| `competitive_analysis` 表未使用 (0 条) | 🟡 中等 | ⚠️ 功能未启用 |
| `negative_sources` 表未使用 (0 条) | 🟡 中等 | ⚠️ 功能未启用 |
| 多版本日志器共存 | 🟡 中等 | ⚠️ 需统一 |

---

## 🔍 存储架构详细分析

### 1. 主数据库 (database.db)

**位置**: `/Users/sgl/PycharmProjects/PythonProject/backend_python/database.db`  
**大小**: 460KB  
**状态**: ✅ 活跃使用

#### 表结构盘点

| 表名 | 记录数 | 用途 | 状态 |
|------|--------|------|------|
| `test_records` | 14 | 存储诊断测试记录 | ✅ 活跃 |
| `users` | - | 用户信息 | ✅ 活跃 |
| `task_statuses` | 0 | 任务状态跟踪 | ⚠️ 未使用 |
| `brand_test_results` | 0 | 品牌测试结果 | ⚠️ 未使用 |
| `competitive_analysis` | 0 | 竞品分析数据 | ⚠️ 未使用 |
| `negative_sources` | 0 | 负面信源数据 | ⚠️ 未使用 |
| `report_metadata` | - | 报告元数据 | ⚠️ 未使用 |
| `audit_logs` | - | 审计日志 | ⚠️ 未使用 |
| `sync_*` | - | 同步相关 | ⚠️ 未使用 |
| `permission_*` | - | 权限相关 | ⚠️ 未使用 |

#### 数据流分析

```
诊断测试完成
   ↓
nxm_execution_engine.py
   ↓
save_test_record() → test_records ✅
   ↓
results_summary (JSON, 压缩)
   ↓
包含：brand_scores, competitive_analysis, negative_sources 等
```

**问题**: 
- `competitive_analysis` 和 `negative_sources` 表存在但从未写入数据
- 数据存储在 `test_records.results_summary` (JSON) 中，而非独立表
- 表设计与实际使用不一致

---

### 2. 品牌测试数据库 (brand_test.db)

**位置**: `/Users/sgl/PycharmProjects/PythonProject/backend_python/wechat_backend/data/brand_test.db`  
**大小**: 572KB  
**状态**: ❌ 完全闲置

#### 表结构盘点

| 表名 | 记录数 | 用途 | 状态 |
|------|--------|------|------|
| `aggregated_results` | 0 | 聚合结果 | ❌ 未使用 |
| `brand_rankings` | 0 | 品牌排名 | ❌ 未使用 |
| `question_stats` | 0 | 问题统计 | ❌ 未使用 |
| `model_stats` | 0 | 模型统计 | ❌ 未使用 |
| `execution_logs` | 0 | 执行日志 | ❌ 未使用 |
| `performance_metrics` | 0 | 性能指标 | ❌ 未使用 |

**问题**:
- **所有表都是空的！**
- 该数据库可能是为"阶段 3"功能预留的
- 当前所有数据都存储在 `database.db` 的 `test_records` 表中
- **建议**: 要么启用该数据库，要么删除以避免混淆

---

### 3. AI 响应日志 (ai_responses.jsonl)

**位置**: `/Users/sgl/PycharmProjects/PythonProject/backend_python/wechat_backend/data/ai_responses/ai_responses.jsonl`  
**大小**: 572KB  
**记录数**: 139 条  
**状态**: ✅ 正常记录

#### 数据格式

```json
{
  "record_id": "cb5431cd-2dfa-4470-8d24-a0854db463d5",
  "timestamp": "2026-02-21T11:29:06.884158",
  "version": "2.0",
  "question": {
    "text": "...",
    "stats": {...}
  },
  "response": {
    "text": "...",
    "stats": {...}
  },
  "platform": {
    "name": "deepseek",
    "model": "deepseek-chat"
  },
  "business": {
    "brand": "华为",
    "competitor": "小米, Oppo, Vivo",
    "industry": "家居定制",
    "question_category": "品牌搜索"
  },
  "performance": {
    "latency_ms": 19809,
    "tokens": {"total": 1227},
    "throughput": 61.94
  },
  "status": {
    "success": true
  },
  "context": {
    "execution_id": "61472016-2f7a-4fa8-a052-367d97f2df7e",
    "question_index": 1,
    "total_questions": 8
  },
  "metadata": {
    "source": "nxm_execution_engine",
    "geo_analysis": {...}
  }
}
```

#### 日志器实现分析

**文件**: `wechat_backend/utils/ai_response_logger_v2.py`

**功能**:
- ✅ 记录 AI 请求和响应
- ✅ 记录性能指标（延迟、吞吐量）
- ✅ 记录 geo_analysis 分析结果
- ✅ 支持查询和统计

**问题**:
- ⚠️ 存在多个版本：`ai_response_logger_v2.py`, `ai_response_logger_enhanced.py`, `ai_response_wrapper.py`
- ⚠️ 代码重复，维护成本高

---

### 4. 缓存数据库 (cache.db)

**位置**: `/Users/sgl/PycharmProjects/PythonProject/backend_python/cache.db`  
**大小**: 20KB  
**表**: `cache_entries`  
**状态**: ⚠️ 需验证使用情况

---

### 5. 小程序本地存储

**类型**: wx.setStorageSync / wx.getStorageSync  
**状态**: ⚠️ 需验证

#### 已识别的 Storage Key

| Key | 用途 | 状态 |
|-----|------|------|
| `latestTestResults_{id}` | 测试结果 | ✅ 已修复 |
| `latestCompetitiveAnalysis_{id}` | 竞品分析 | ✅ 已修复 |
| `latestBrandScores_{id}` | 品牌分数 | ✅ 已修复 |
| `latestSemanticDrift_{id}` | 语义偏移 | ✅ 已修复 |
| `latestRecommendations_{id}` | 优化建议 | ✅ 已修复 |
| `latestNegativeSources_{id}` | 负面信源 | ✅ 已修复 |
| `latestTargetBrand` | 目标品牌 | ✅ 已修复 |
| `latestCompetitorBrands` | 竞品品牌 | ✅ 已修复 |

---

## 🔴 关键问题识别

### 问题 1: brand_test.db 完全闲置

**现象**: 所有表都是 0 条记录

**根因**:
1. 代码中引用了该数据库的表结构（schema）
2. 但实际写入操作都指向 `database.db`
3. 可能是"阶段 3"功能的预留，但尚未实现

**影响**:
- 数据分散风险（如果启用）
- 维护混淆（开发者不知道用哪个库）
- 磁盘空间浪费

**建议**:
- **选项 A**: 删除 `brand_test.db` 及相关 schema 定义
- **选项 B**: 实现"阶段 3"功能，启用该数据库

---

### 问题 2: 表设计与实际使用不一致

**现象**:
```sql
-- 设计了独立表
CREATE TABLE competitive_analysis (...);  -- 0 条记录
CREATE TABLE negative_sources (...);       -- 0 条记录

-- 但数据存在 JSON 中
SELECT json_extract(results_summary, '$.competitive_analysis') FROM test_records;
```

**根因**:
- 设计时计划使用独立表
- 实现时为了简化，使用 JSON 存储
- 表结构保留但未使用

**影响**:
- 查询性能（JSON 查询 vs 表查询）
- 数据完整性（无外键约束）
- 维护混淆

**建议**:
- **短期**: 删除未使用的表定义
- **长期**: 如需要独立表，迁移数据并更新代码

---

### 问题 3: 多版本日志器共存

**现象**:
```
ai_response_logger_v2.py       # v2 版本
ai_response_logger_enhanced.py # 增强版
ai_response_wrapper.py         # 包装器
```

**根因**:
- 迭代开发过程中创建了多个版本
- 未统一清理旧版本

**影响**:
- 代码维护成本高
- 开发者困惑（用哪个？）
- 潜在的 bug（不同版本行为不一致）

**建议**:
- 统一使用一个版本（推荐 `ai_response_logger_v2.py`）
- 删除其他版本或标记为废弃

---

### 问题 4: task_statuses 表未使用

**现象**: `task_statuses` 表有 0 条记录

**根因**:
- 代码中使用 `execution_store` (内存) 存储任务状态
- 数据库表作为持久化备份，但未实现写入逻辑

**影响**:
- 服务器重启后任务状态丢失
- 无法查询历史任务状态

**建议**:
- 实现任务状态持久化逻辑
- 或在代码中明确标记为"未来功能"

---

## 📊 数据流完整性分析

### 诊断测试数据流

```
用户启动诊断
   ↓
首页 (index.js)
   ↓
POST /api/perform-brand-test
   ↓
nxm_execution_engine.py
   ↓
[AI 调用] → ai_responses.jsonl ✅
   ↓
[结果聚合]
   ↓
[保存到数据库]
   ├─ test_records ✅
   ├─ competitive_analysis ❌ (未写入)
   └─ negative_sources ❌ (未写入)
   ↓
[返回前端]
   ↓
首页轮询 /test/status/{id}
   ↓
结果页 (results.js)
   ↓
[保存到本地存储] ✅
```

**断裂点**:
1. `competitive_analysis` 和 `negative_sources` 未写入独立表
2. `task_statuses` 未持久化
3. `brand_test.db` 完全未使用

---

## 🎯 优化升级方案

### 阶段 1: 清理与统一 (立即执行)

#### 任务 1.1: 删除闲置数据库

```bash
# 删除 brand_test.db
rm /Users/sgl/PycharmProjects/PythonProject/backend_python/wechat_backend/data/brand_test.db

# 删除相关 schema 定义
# 编辑：wechat_backend/phase3_database_schema.sql
# 标记为"未来功能"或删除
```

#### 任务 1.2: 删除未使用的表定义

```sql
-- 在 database.db 中
DROP TABLE IF EXISTS competitive_analysis;
DROP TABLE IF EXISTS negative_sources;
DROP TABLE IF EXISTS report_metadata;
DROP TABLE IF EXISTS task_statuses;
DROP TABLE IF EXISTS brand_test_results;
```

#### 任务 1.3: 统一日志器

```bash
# 保留 ai_response_logger_v2.py
# 删除或标记其他版本
mv ai_response_logger_enhanced.py ai_response_logger_enhanced.py.deprecated
mv ai_response_wrapper.py ai_response_wrapper.py.deprecated
```

---

### 阶段 2: 增强与优化 (近期规划)

#### 任务 2.1: 实现任务状态持久化

```python
# 在 nxm_execution_engine.py 中
def save_task_status(execution_id, status, progress, stage):
    """保存任务状态到数据库"""
    conn = get_connection()
    cursor = conn.cursor()
    cursor.execute("""
        INSERT OR REPLACE INTO task_statuses 
        (task_id, status, progress, stage, updated_at)
        VALUES (?, ?, ?, ?, CURRENT_TIMESTAMP)
    """, (execution_id, status, progress, stage))
    conn.commit()
```

#### 任务 2.2: 优化 JSON 查询性能

```python
# 添加索引加速 JSON 查询
CREATE INDEX IF NOT EXISTS idx_test_records_execution_id 
ON test_records (json_extract(results_summary, '$.execution_id'));

CREATE INDEX IF NOT EXISTS idx_test_records_brand 
ON test_records (json_extract(results_summary, '$.brand_name'));
```

#### 任务 2.3: 添加数据归档机制

```python
# 定期将旧数据归档到单独的文件
def archive_old_records(days=30):
    """归档 30 天前的记录"""
    # 导出到 JSON/CSV
    # 从主表删除
    pass
```

---

### 阶段 3: 监控与告警 (长期规划)

#### 任务 3.1: 添加存储监控

```python
# 监控数据库大小
def check_database_size():
    size = os.path.getsize('database.db')
    if size > 100 * 1024 * 1024:  # 100MB
        send_alert('数据库过大')

# 监控日志文件
def check_log_file():
    with open('ai_responses.jsonl') as f:
        lines = sum(1 for _ in f)
    if lines < 10:  # 日志过少，可能记录失败
        send_alert('AI 响应日志记录异常')
```

#### 任务 3.2: 添加数据完整性检查

```python
# 定期检查数据一致性
def check_data_integrity():
    # test_records 与 ai_responses.jsonl 对比
    # 检查缺失的记录
    pass
```

---

## 📋 验证清单

### 清理后验证

- [ ] `brand_test.db` 已删除
- [ ] 未使用的表已删除
- [ ] 日志器已统一
- [ ] 代码引用已更新
- [ ] 测试通过

### 功能验证

- [ ] 诊断测试正常
- [ ] AI 响应正常记录
- [ ] 结果页正常显示
- [ ] 本地存储正常
- [ ] 无数据丢失

---

## 📊 存储架构对比

| 维度 | 修复前 | 修复后 |
|------|--------|--------|
| 数据库数量 | 3 个 | 2 个 (-33%) |
| 活跃表 | 6 个 | 6 个 |
| 闲置表 | 10+ 个 | 0 个 (-100%) |
| 日志器版本 | 3 个 | 1 个 (-67%) |
| 代码清晰度 | ⚠️ 混乱 | ✅ 清晰 |
| 维护成本 | 高 | 低 |

---

## 🎯 总结

### 已完成分析

- ✅ 全面盘点所有数据存储
- ✅ 识别 4 个关键问题
- ✅ 设计 3 阶段优化方案
- ✅ 提供详细验证清单

### 待执行任务

| 任务 | 优先级 | 预计工时 |
|------|--------|----------|
| 删除闲置数据库 | P1 | 30 分钟 |
| 删除未使用表 | P1 | 30 分钟 |
| 统一日志器 | P2 | 1 小时 |
| 实现任务状态持久化 | P2 | 2 小时 |
| 添加存储监控 | P3 | 4 小时 |

---

**报告生成时间**: 2026-02-22 16:00:00  
**下一步**: 执行阶段 1 清理任务

---

*数据存储架构盘点及优化升级报告，全面分析当前存储状况并提供优化方案*
