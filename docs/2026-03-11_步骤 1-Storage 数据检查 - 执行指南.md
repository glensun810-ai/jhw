# 步骤 1: Storage 数据检查 - 执行指南

**文档编号**: STEP1-2026-03-11-001  
**执行时间**: 预计 15 分钟  
**状态**: ⏳ 准备执行  

---

## 一、执行前准备

### 1.1 确认环境

- [ ] 微信开发者工具已打开
- [ ] 小程序已编译运行
- [ ] 控制台面板已打开

### 1.2 执行诊断（如果还没有）

```
1. 打开小程序首页
2. 输入品牌名称（如：华为）
3. 点击"开始诊断"
4. 等待诊断完成
5. 不要关闭报告页
```

---

## 二、执行检查脚本

### 2.1 方法一：使用检查脚本（推荐）

**步骤**:

1. **复制脚本**: `docs/debug-scripts/step1-storage-check.js`

2. **粘贴到控制台**:
   ```
   微信开发者工具 → 调试器 → Console 面板
   → 粘贴完整脚本 → 回车执行
   ```

3. **查看输出结果**:
   ```
   期望看到:
   ========================================
   📊 Storage 数据检查 - 步骤 1
   ========================================
   
   【1】获取所有 Storage Keys
   所有 keys 数量：XX
   所有 keys: [...]
   
   【2】查找诊断相关 Keys
   diagnosis_result_ 开头的 keys: ['diagnosis_result_xxx']
   数量：1
   
   【3】检查诊断历史列表
   历史记录数量：1
   最新记录：{...}
   
   【4】查看最新诊断数据
   === 诊断数据详情 ===
   Key: diagnosis_result_xxx
   ExecutionId: xxx
   BrandName: 华为
   
   === 核心数据检查 ===
   ✅ 品牌分布数据：存在
      total_count: 10
      data keys: ['华为', '小米', ...]
   ✅ 详细结果：存在，数量 = 20
   
   === 下一步建议 ===
   ➡️  继续步骤 2: 检查页面加载逻辑
   ```

### 2.2 方法二：手动检查

**步骤**:

1. **在控制台执行**:
   ```javascript
   // 1. 查看所有 keys
   const info = wx.getStorageInfoSync();
   console.log('所有 keys:', info.keys);
   
   // 2. 查找诊断 keys
   const diagnosisKeys = info.keys.filter(k => k.startsWith('diagnosis_result_'));
   console.log('诊断 keys:', diagnosisKeys);
   
   // 3. 查看最新数据
   if (diagnosisKeys.length > 0) {
     const data = wx.getStorageSync(diagnosisKeys[0]);
     console.log('诊断数据:', data);
     console.log('brandDistribution:', data.data?.brandDistribution);
     console.log('brandScores:', data.data?.brandScores);
     console.log('detailedResults:', data.data?.detailedResults?.length);
   }
   ```

---

## 三、结果判断

### 3.1 正常结果 ✅

```javascript
// 输出示例
诊断 keys: ['diagnosis_result_xxx']
诊断数据：{
  version: '1.0',
  executionId: 'xxx',
  brandName: '华为',
  data: {
    brandDistribution: {
      total_count: 10,
      data: {
        '华为': {count: 5, percentage: 50},
        '小米': {count: 3, percentage: 30}
      }
    },
    brandScores: {...},
    detailedResults: [/* 20 条结果 */],
    competitiveAnalysis: {...},
    qualityScore: {...},
    overallScore: 75
  }
}

✅ 品牌分布数据：存在
   total_count: 10
   data keys: ['华为', '小米', ...]
✅ 详细结果：存在，数量 = 20

➡️  继续步骤 2: 检查页面加载逻辑
```

**判断**: Storage 数据正常，进入步骤 2

---

### 3.2 异常情况 A: Storage 无数据 ❌

```javascript
// 输出示例
诊断 keys: []
❌ 未找到诊断数据！

可能原因:
  1. 未执行诊断
  2. 诊断完成后未保存到 storage
  3. storage key 命名不一致

➡️  检查 pages/index/index.js 的 handleDiagnosisComplete 函数
➡️  确认 saveDiagnosisResult 是否被调用
```

**下一步**: 检查保存逻辑

**检查文件**: `pages/index/index.js`

**检查点**:
```javascript
// handleDiagnosisComplete 函数中
const saveSuccess = saveDiagnosisResult(executionId, {
  brandName: this.data.brandName,
  // ... 其他数据
  rawResponse: parsedStatus
});

console.log('Storage 保存结果:', saveSuccess);
```

**修复方案**:
```javascript
// 如果未调用 saveDiagnosisResult
// 添加保存逻辑
const { saveDiagnosisResult } = require('../../utils/storage-manager');
const saveSuccess = saveDiagnosisResult(executionId, {
  brandName: this.data.brandName,
  competitorBrands: this.data.competitorBrands || [],
  selectedModels: parsedStatus.selectedModels || [],
  customQuestions: parsedStatus.customQuestions || [],
  completedAt: new Date().toISOString(),
  status: 'completed',
  overallScore: parsedStatus.overall_score || 0,
  results: parsedStatus.detailed_results || [],
  detailedResults: parsedStatus.detailed_results || [],
  brandDistribution: parsedStatus.brand_distribution || {},
  brandScores: parsedStatus.brand_scores || {},
  rawResponse: parsedStatus
});
```

---

### 3.3 异常情况 B: brandDistribution 为空 ❌

```javascript
// 输出示例
=== 核心数据检查 ===
❌ 问题发现：brandDistribution 和 brandScores 均为空！
   可能原因：后端未生成品牌分布数据
   下一步：检查后端诊断编排器

➡️  跳转到步骤 4: 检查后端数据生成
```

**下一步**: 检查后端数据生成

**检查文件**: `backend_python/wechat_backend/services/diagnosis_orchestrator.py`

**检查点**:
```python
# 在诊断编排器中
def _generate_brand_distribution(self, results):
    """生成品牌分布数据"""
    print(f'[诊断编排器] 生成品牌分布，results 数量：{len(results)}')
    
    # 检查是否有数据
    if not results:
        print('[诊断编排器] ❌ results 为空！')
        return {'total_count': 0, 'data': {}}
    
    # 统计品牌出现次数
    brand_counts = {}
    for result in results:
        brand = result.get('brand', 'Unknown')
        brand_counts[brand] = brand_counts.get(brand, 0) + 1
    
    print(f'[诊断编排器] 品牌统计：{brand_counts}')
    
    total = sum(brand_counts.values())
    distribution = {
        'total_count': total,
        'data': {
            brand: {
                'count': count,
                'percentage': round(count / total * 100, 2)
            }
            for brand, count in brand_counts.items()
        }
    }
    
    print(f'[诊断编排器] 品牌分布：{distribution}')
    return distribution
```

---

### 3.4 异常情况 C: detailedResults 为空 ❌

```javascript
// 输出示例
=== 核心数据检查 ===
✅ 品牌分布数据：存在
❌ 问题发现：detailedResults 为空！
   可能原因：诊断结果为空或保存失败

➡️  跳转到步骤 5: 检查诊断执行流程
```

**下一步**: 检查诊断执行流程

**检查后端日志**:
```bash
cd backend_python
tail -f logs/app.log | grep "diagnosis"
```

**期望日志**:
```
[诊断编排器] 开始执行诊断，execution_id=xxx
[诊断编排器] AI 适配器执行完成，响应数量=20
[诊断编排器] 生成品牌分布，results 数量=20
[诊断编排器] 品牌统计：{'华为': 5, '小米': 3, ...}
[诊断编排器] 诊断完成，保存到数据库
```

---

## 四、检查结果记录

### 4.1 检查记录表

| 检查项 | 结果 | 说明 |
|-------|------|------|
| Storage keys 数量 | ⏳ 待检查 | - |
| diagnosis_result_ keys | ⏳ 待检查 | - |
| 最新诊断数据 | ⏳ 待检查 | - |
| brandDistribution | ⏳ 待检查 | - |
| brandScores | ⏳ 待检查 | - |
| detailedResults | ⏳ 待检查 | - |

### 4.2 问题记录

| 问题 | 发现时间 | 状态 |
|-----|---------|------|
| - | - | - |

---

## 五、下一步

### 5.1 结果正常 ✅

```
➡️  继续步骤 2: 检查页面加载逻辑
```

**操作**:
1. 打开 `miniprogram/pages/report-v2/report-v2.js`
2. 添加调试日志
3. 重新编译查看日志

### 5.2 结果异常 ❌

```
➡️  根据异常类型跳转到对应步骤
   - Storage 无数据 → 检查保存逻辑
   - brandDistribution 为空 → 步骤 4
   - detailedResults 为空 → 步骤 5
```

---

## 六、快速复制的脚本

```javascript
// 快速检查（简化版）
(function() {
  console.log('📊 Storage 快速检查\n');
  const info = wx.getStorageInfoSync();
  const keys = info.keys.filter(k => k.startsWith('diagnosis_result_'));
  console.log('诊断 keys:', keys);
  if (keys.length > 0) {
    const data = wx.getStorageSync(keys[0]);
    console.log('brandDistribution:', data.data?.brandDistribution ? '✅ 存在' : '❌ 不存在');
    console.log('brandScores:', data.data?.brandScores ? '✅ 存在' : '❌ 不存在');
    console.log('detailedResults:', data.data?.detailedResults?.length || 0, '条');
    console.log('\n下一步:', 
      (!data.data?.brandDistribution && !data.data?.brandScores) ? '→ 步骤 4' :
      (!data.data?.detailedResults || data.data.detailedResults.length === 0) ? '→ 步骤 5' :
      '→ 步骤 2'
    );
  } else {
    console.log('❌ 未找到诊断数据');
  }
})();
```

---

**执行时间**: 2026-03-11  
**执行人**: 系统首席架构师  
**状态**: ⏳ 准备执行  

---

**🎯 立即执行：复制脚本到控制台，查看结果！**
