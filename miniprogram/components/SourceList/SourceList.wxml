<!-- components/SourceList/SourceList.wxml -->
<view class="source-list">
  <view class="list-header">
    <text class="list-title">{{title}}</text>
    <text class="list-count" wx:if="{{sources.length > 0}}">({{sources.length}})</text>
  </view>

  <skeleton type="paragraph" rows="5" loading="{{loading}}">
    <!-- 信源列表 -->
    <view class="source-items" wx:if="{{!loading && sources.length > 0}}">
      <view
        class="source-item"
        wx:for="{{sources}}"
        wx:key="index"
        data-index="{{index}}"
        bindtap="onSourceTap">
        <view class="source-content">
          <view class="source-main">
            <text class="source-title">{{item.title || item.url || '未知信源'}}</text>
            <text class="source-url" wx:if="{{item.url}}">{{item.url}}</text>
          </view>
          <view class="source-actions">
            <view class="copy-btn" data-index="{{index}}" bindtap="onCopyLink">
              <text class="copy-icon">📋</text>
            </view>
          </view>
        </view>
        <view class="source-meta">
          <text class="source-score" wx:if="{{item.score}}">可信度: {{item.score}}</text>
          <text class="source-type" wx:if="{{item.type}}">{{item.type}}</text>
        </view>
      </view>
    </view>

    <!-- 防御性渲染：当数据为空时显示占位符 -->
    <view class="placeholder-container" wx:if="{{!loading && (!sources || sources.length === 0)}}">
      <text class="placeholder-text">暂无信源数据</text>
    </view>
  </skeleton>
</view>